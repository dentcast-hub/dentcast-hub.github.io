---
description: DentCast Plus / Premium surfaces — learning pathways, collections, Leitner flashcards, the scored quiz bank, the میز کار content index, the premium paper library (cabinet), and the new-article push. Use when working on anything under plus/ or plus-api/, on the cabinet catalogue, or when a publish must update a Plus/Premium artifact.
globs: plus/**,plus-api/**,tools/build_plus_index.mjs,tools/build_flashcards_index.mjs,tools/build_quiz_index.mjs,tools/pathway_*.py,dentcast_cabinet_full_catalog*.json,dentcast_cabinet_search.html
alwaysApply: false
---

# Plus / Premium — Cursor

نقشهٔ کامل در `AGENTS.md` بخش ۷. منطقِ گام‌ها در `.dentcast/workflows/README.md`.

## چه چیزی با هر publish به‌روز می‌شود

| سطح | آرتیفکت | گامِ ورکفلو | بیلدر (تنها نویسنده) |
|---|---|---|---|
| Plus | درخت ناوبریِ «میز کار» | همیشه (brain تغییر کرده) | `node tools/build_plus_index.mjs` → `plus/content-index.json` |
| Plus/Premium | فلش‌کارت Leitner | `4.11` — `DefinedTermSet` در `plus/faq-corpus.json` (نه روی خودِ صفحه) | `node tools/build_flashcards_index.mjs` → `plus/flashcards-index.json` |
| Plus/Premium | کوییز نمره‌دار | `4.12` — `FAQPage` در `plus/faq-corpus.json` (نه روی خودِ صفحه) | `node tools/build_quiz_index.mjs` → `plus/quiz-index.json` |
| Premium | مسیر یادگیری | `5.6` | `python3 tools/pathway_place.py --insert …` → `plus/pathways.json` |
| Premium | باندلِ شروع (`kind: "bundle"`) | `5.6-ب` (زنجیرهٔ پنج‌گیتی) | همان ابزار، `--pathway <bundle-id>` + جاروی دستیِ `plus/js/home-bundles.js` + `asset_version.py --bump` |
| Plus/Premium | نوتیف مطلب جدید | فاز E | `<meta name="dc-notify" content="true">` در `<head>` |
| Premium | کالکشن‌ها | — | مالِ کاربر است؛ publish کاری با آن ندارد |
| Premium | کتابخانهٔ کابینت | `4.10` (فقط با پیپر پیوست) | ندارد — `dentcast_cabinet_full_catalog.json` دستی طبق گام ۴.۱۰ |

**هیچ‌کدام از فایل‌های ایندکس را دستی ویرایش نکن** — `content-index.json`،
`flashcards-index.json`، `quiz-index.json` تماماً تولیدی‌اند. تنها فایل Plus که
دستی (از طریق ابزارش) نوشته می‌شود `plus/pathways.json` است.

## قواعدی که زیاد اشتباه می‌شوند

- **pathway ≠ pillar.** pillar خانهٔ آیتم است (دقیقاً یکی، گام ۲.۴). pathway یک
  سفر یادگیری است و **یک آیتم می‌تواند در چند pathway باشد**، هر بار در جای درستِ
  پیش‌نیاز→پیشرفته. pillarِ آیتم هیچ نقشی در انتخاب pathway ندارد. تصمیم
  **معنایی** است؛ مرزی بود، بپرس.
  ```bash
  python3 tools/pathway_place.py <content_id>              # کاندیدها + anchor پیشنهادی
  python3 tools/pathway_place.py --insert <content_id> --pathway <id> --after <anchor> [--milestone]
  python3 tools/pathway_place.py --coverage                # تأیید
  ```
  ویرایش `plus/pathways.json` دیگر redeploy API نمی‌خواهد — `content-refresh.ts`
  فایل منتشرشده را هر چند دقیقه در حال اجرا دوباره می‌گیرد (validated، فقط
  upgrade، و در هر خطا آخرین نسخهٔ سالم). ولی «بدون redeploy» یعنی **زنده**، نه
  «موقت»: `/plus/pathways.html`، `/plus/pathway.html?id=` و بلوکِ «مسیر یادگیری»
  پیشخوان همین فایل را رندر می‌کنند، پس جای‌گذاریِ غلط را مشترکِ واقعی می‌بیند.
  قبل از commit حتماً `python3 -m json.tool plus/pathways.json > /dev/null`.
- **باندل ≠ مسیر.** باندل‌های شروع (`kind: "bundle"`) در همان `pathways.json`
  می‌نشینند ولی زیرمجموعه‌ای ۵ تا ۸ قدمیِ «فقط هستهٔ» یک مسیرند، نه مسیرِ تازه.
  گام ۵.۶ آیتم را در **مسیرهای کامل** جا می‌دهد؛ ورود به باندل کارِ گام
  **5.6-ب** است: پنج گیتِ مکانیکی با **پیش‌فرضِ «نه»** (نوع پوشه ∈ episodes/
  notecast/sharehub/dentai · عنوان یک مفهومِ کامل، نه کیس/نکته · پرکردنِ حفره‌ای
  که هیچ قدمِ فعلی نمی‌پوشاند · نبودنِ دوقلو · ≤ ۸ قدم بعد از درج). هر ابهامی =
  **سوال از بنیان‌گذار**. درج فقط **وسطِ لیست** با `--after <anchor>` — هرگز
  `--at-end`، چون قدمِ آخرِ باندل تنها حاملِ `milestone: true` (پرچمِ 🏁) است.
  دو آینهٔ دستی که هر ویرایشِ باندل باید در همان کامیت جارو کند: شمارهٔ قدم در
  `plus/js/home-bundles.js` و بعدش `python3 tools/asset_version.py --bump`.
  کاتالوگ و دکترینِ «چه چیزی وارد باندل می‌شود»: `.dentcast/bundles-handoff.md`.
  حکمِ هر باندلِ کاندید (وارد شد / رد شد با شمارهٔ گیت / سوال شد) در گزارشِ
  انتشار می‌آید.
- **فلش‌کارت یک مفهوم است، نه یک سوال.** `DefinedTerm` اتمی؛ کپیِ کلمه‌به‌کلمهٔ
  FAQ ممنوع؛ سوال‌های مقایسه‌ای/تصمیمی کارت نمی‌شوند (و اسکیپشان گزارش می‌شود).
  `@id`ها بعد از انتشار **پایدار**اند (رکوردهای `card_state` به همان fragment
  گره خورده‌اند) — با اصلاح متن، شماره‌گذاری را جابه‌جا نکن.
- **کوییز فقط سوال‌های باینری را برمی‌دارد** که پاسخشان با «بله»/«خیر» شروع شود.
  سوال باید standalone باشد — «این کیس»/«این مطالعه» ممنوع. «بستگی دارد»ِ صادق
  را به بله/خیرِ دروغین تبدیل نکن؛ درست است که از بانکِ نمره‌دار بیرون بماند.
- **کتابخانه سه قید دارد که publish نباید خرابشان کند.** از دسترسِ موتورهای
  جستجو بیرون است (`robots.txt` هر دو کاتالوگ را Disallow می‌کند، `_headers`
  هدرِ `X-Robots-Tag: noindex` می‌دهد، صفحه هم `<meta robots noindex>` دارد) —
  «درستش» نکن و واردِ sitemap نکن. روی سایت پریمیوم است و گیتِ تپ در
  `plus/js/library-gate.js` است (delegated، به **مقصد** بسته نه به یک المان، و
  از `plus/js/header.js` روی هر صفحه نصب می‌شود)، پس پیپر جدید را با باز کردنِ مستقیمِ
  `dentcast_cabinet_search.html` تأیید کن، نه با زدنِ کارت. و شمارِ مقالاتِ
  روی کارت و شیتِ گیت **دستی** است: با عبور از هر صدِ رُند، همهٔ نسخه‌هایش را
  با هم جارو کن — در **دو** فایل، نه یکی
  (`grep -rn '۲۲۰۰' index.html plus/js/library-gate.js`) و بعدش
  `python3 tools/asset_version.py --bump`، چون متنِ شیت در یک ماژولِ مشترک است.
- **نشانِ اهدایی از ادمین اهدا می‌شود، نه از دست‌کاریِ داده.** دیوارِ «افتخارات»
  (`plus/badges.json`) تماماً مشتق است؛ تنظیمِ متن/آستانهٔ یک نشان = کامیت به
  سایت. کلاسِ اهدایی — نشانی که متریکش `grant:<کلید خودش>` است؛ «همراه» (کلید
  `companion`) اولین عضو — را فقط از `POST /admin/badges/grant` بده (فرمِ
  «اهدای نشان» روی صفحهٔ `GET /admin`؛ پس‌گرفتن با `/admin/badges/revoke`)؛
  همان اکشن تخفیفِ یک‌بارمصرفِ اختیاری را هم در همان تراکنش می‌سازد (عرفِ
  گزارشِ باگ ٪۵). نشانِ اهداییِ جدید در `badges.json` همیشه `earned_only` و
  تک‌سطحی است و هرگز `discount_percent` روی خودش ندارد؛ و افزودنِ نشانِ
  اهداییِ *کلاس‌ساز* (متریک/سرویس جدید) rebuild کانتینر API می‌خواهد، نه فقط
  کامیت. جزئیات: بولتِ `plus/badges.json` در `AGENTS.md` و
  `plus-api/src/services/badge-grants.ts`.
- **صندلیِ «ستون» هم اهدا می‌شود، ولی از درِ خودش.** صندلی از رتبهٔ اولین
  پرداختِ موفق در `payments` مشتق است؛ هدیهٔ ادمین/گیفت‌کارت/جایزهٔ لیگ صندلی
  نمی‌سازند. برای کسی که در ساختنِ سایت کمک کرده و هرگز در دفترِ پرداخت‌ها
  ظاهر نمی‌شود، **فقط** `POST /admin/pillar/grant` (پس‌گرفتن با
  `/admin/pillar/revoke`) — هرگز ردیفِ `payments` دستی نساز، که به درآمد، به
  سقفِ ماهانهٔ درگاه و به joinِ اعتبارهای تخفیف دروغ می‌گوید. اهدا یکی از ۵۱
  صندلی را **خرج** می‌کند (ظرفیت بزرگ نمی‌شود)، جای هیچ‌کس را عوض نمی‌کند، و
  خریدِ **اولِ** دارنده‌اش هم ٪۲۰ تخفیف می‌خورد. `seated: false` در پاسخ خطا
  نیست: یعنی ردیف نوشته شد ولی صندلی‌ها تمام بوده — بخوانش. جزئیات: بولتِ
  `plus/badges.json` در `AGENTS.md` و `plus-api/src/services/pillar.ts`
  (`SEAT_CLAIMS`).
- **اطلاعیهٔ یک نفر با broadcast فرستاده نمی‌شود.** باریک‌ترین مخاطبِ
  `POST /admin/notices/broadcast` یک تیرِ کامل است (همه/رایگان/پریمیوم)، پس
  تشکر از یک نفر با آن یعنی گفتن به همهٔ آن تیر. برای یک خواننده
  `POST /admin/notices/user`: ردیفِ اطلاعیه در هر ساعتی فوراً می‌نشیند، `push`
  مگر با `force` پنجرهٔ بیداری را رعایت می‌کند، و از بودجهٔ روزانهٔ پوشِ او
  خرج نمی‌شود. hold ندارد — پوشِ خارج از ساعت `push_skipped` گزارش می‌شود، نه
  صف. جزئیات: بولتِ «اطلاعیه» در `AGENTS.md`.
- **LiteCast از همهٔ این‌ها بیرون است** (بیمارمحور است): نه فلش‌کارت، نه کوییز،
  نه pathway، نه نوتیف. هر بار به‌صورت «اسکیپ — LiteCast» گزارش می‌شود.

## رنگِ پریمیوم

- **کهربایی رنگِ پریمیوم است و فقط همان.** کهربایی/طلایی (`#c9922b` روشن،
  `#e3b849` تیره) فقط روی چیزی می‌نشیند که اشتراک می‌خرد: کارت‌های قفل‌شده‌ی
  «دنت‌کست پریمیوم»، بَج «🔒 ویژه‌ی پریمیوم»، و کارت کتابخانه در آرشیو. بقیه‌ی
  سایت آبی می‌ماند. **پالس** خبر است نه پریمیوم (آبی می‌ماند)، و **«دنت‌کست
  پلاس» بخشِ ویدیوی رایگان است نه سطح پولی** (پلاس = واردشده‌ی رایگان،
  پریمیوم = پولی) — چیپش هرگز کهربایی نمی‌شود. توکن‌ها: `--x-amber*` در
  `<style id="dcDepthTheme">` فایل `index.html`. شرح کامل در `CLAUDE.md` و
  `AGENTS.md` (بولتِ «Amber is the PREMIUM colour»).
