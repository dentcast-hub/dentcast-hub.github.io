.claude/*
!.claude/skills/
__pycache__/
*.pyc

# Cabinet enrichment resume cache (regenerable; never commit)
enrich_cache.json

# Dev-only screenshot output (probe/shot scripts); the scripts stay, PNGs don't.
scripts-dev/*.png
