# AGENTS.md — DentCast Repo Protocol (agent-neutral)

این فایل نسخهٔ **مستقل از ایجنتِ** `CLAUDE.md` است. هر ایجنتی که این ریپو را
باز می‌کند — Cursor، Codex/GPT، Claude Code، یا هر چیز دیگری — باید این فایل را
بخواند و **دقیقاً** طبق آن عمل کند. محتوای این فایل با `CLAUDE.md` یکی است؛ هر
دو با هم به‌روز می‌شوند (جدول نگهداری در انتهای فایل).

> **دو فایلی که قبل از هر کاری روی محتوا باید خوانده شوند:**
> ۱. همین فایل (نقشه و تریگرها)
> ۲. `.dentcast/workflows/agent-parity.md` (قانونِ رفتاری مشترک — چطور خروجیِ
>    هر ایجنت عیناً مثل خروجی Claude Code در بیاید)
>
> بعد، فایل کانونیِ همان مسیر را **کامل** بخوان. روترها جایگزین آن نیستند.

---

## ۰. قانون طلایی

منطق کار **فقط** در فایل‌های کانونی زیر است. این فایل، `CLAUDE.md`، و
`.cursor/rules/*.mdc` صرفاً **روتر**اند: مسیر را نشان می‌دهند، منطق را تکرار
نمی‌کنند و جایگزین خواندنش نمی‌شوند.

| اگر درخواست کاربر... | فایل کانونی | تریگر نمونه |
|---|---|---|
| **محتوای جدید** برای انتشار دارد (هر نوع: NoteCast، Insight، LiteCast، DentAI، Promptologist، MetaNote، Chairside، PhotoCast، ShareHub، DentCast+، اپیزود، Glossary…) | `.dentcast/workflows/README.md` | «متن جدید دارم»، «این رو منتشر کن» |
| **نسخهٔ انگلیسیِ یک صفحهٔ موجود** را می‌خواهد | `.dentcast/workflows/en-version.md` | «پرامپتولوژیست ۱ رو انگلیسی کن»، «نسخهٔ انگلیسیِ متانوت ۳» |
| **تبلیغ/آگهی** دارد یا می‌خواهد تبلیغی را مدیریت کند | `.dentcast/workflows/ads.md` | «تبلیغ دارم»، «اسپانسر گرفتیم»، «تبلیغ X رو خاموش کن» |
| **آمار تبلیغ** می‌خواهد | `.dentcast/workflows/spot-report.md` | «گزارش تبلیغ بده»، «کلیک اسپانسر X چقدر بوده؟» |
| **ترم دانشنامه** می‌خواهد نوشته شود | `.claude/skills/glossary-writer/SKILL.md` | «ترم Ferrule Effect رو بنویس» |
| **فایل مقاله/پیپر** پیوست کرده (هر نوع محتوا، با یا بدون متن) | `.dentcast/workflows/README.md` — فاز ۰ (روتینگ)، سوال ۴.۷ (مسلح‌شدن)، **گام ۴.۱۰** (سه اکشن) | پیوست‌کردن PDF |

اگر درخواست به هیچ ردیفی نمی‌خورد یا بین چند ردیف مبهم است، **بپرس** — حدس نزن.

> **مسیر فایل‌ها روی گیت‌هاب (اگر لوکال نداری):**
> `https://github.com/dentcast-hub/dentcast-hub.github.io/blob/main/<path>`

---

## ۱. پروتکل انتشار محتوا

وقتی کاربر محتوای جدیدی برای انتشار می‌آورد:

1. `.dentcast/workflows/README.md` را کامل بخوان و **مو به مو** اجرا کن.
2. `dentcast-brain.json` منبع حقیقتِ ساختار، اسکیما و دستهٔ هر نوع محتواست.
   همیشه **آخرین انتریِ هم‌دسته** را الگوی انتری جدید بگیر.
3. **هرگز دسته‌ها را قاطی نکن.** NoteCast در NoteCast می‌ماند، Insight در
   Insight. هر دسته انتری‌اش در بخش خودش و فایلش در دایرکتوری خودش.
4. ویجت «آخرین محتوا» صفحهٔ اصلی، **انتهای** `dentcast-brain.json` را می‌خواند.
   پس هر انتری جدید باید به **انتهای** آرایه اضافه شود — هرگز وسط لیست.
5. هرچه می‌توانی خودت کشف کن؛ فقط چیزی را بپرس که واقعاً قابل استنتاج نیست.
6. **اسکیمای انتری مقدس است.** فیلدی که در انتری‌های قبلیِ همان دسته نبوده،
   اضافه نکن. شکل موجود را عیناً تکرار کن.
7. **رشته‌ای که کاربر داده کپی می‌شود، نه بازتایپ** (Hard Rule 16). نیم‌فاصله
   (`U+200C`) فاصله نیست و فاصله نیم‌فاصله نیست؛ «اندو‌شده» ≠ «اندو شده». اضافه
   یا کم‌کردن یک عبارت به عنوان/کپشنِ کاربر — حتی وقتی دقیق‌تر است — بازنویسی
   است، نه کپی: پیشنهادش بده و تأیید بگیر.
8. **اصلاحِ متن یک «جاروب» است، نه یک ادیتِ نقطه‌ای** (Hard Rule 17). یک عنوان
   دست‌کم روی ۸ سطح زندگی می‌کند (brain، `<title>`، `<h1>`، `og:title`،
   `twitter:title`، `headline`، برچسبِ صفحهٔ فهرست، کارتِ ریلِ صفحهٔ اصلی) و یک
   کپشن روی ۴ (brain، `description`، `og:description` و `twitter:description` —
   روی صفحهٔ fa **و** آینهٔ en). هر اصلاح، همهٔ سطوح را در **همان کامیت** عوض
   می‌کند؛ بعدش بیلدرها و **فاز F** دوباره اجرا می‌شوند.
9. **فاز F — گیت مکانیکی.** publish وقتی تمام است که این سبز باشد:
   ```bash
   python3 tools/verify_publish.py <content_id> \
     --expect-title "<عنوان دقیقاً همان‌طور که کاربر نوشته>" \
     --expect-caption "<کپشن دقیقاً همان‌طور که کاربر نوشته>"
   ```
   خروجیِ غیرصفر یعنی publish ناقص است. هر ردیفِ `FAIL` دستورِ رفعش را خودش چاپ
   می‌کند. خطِ خلاصه‌اش عیناً در گزارش نهایی می‌آید.

---

## ۲. پروتکل نسخهٔ انگلیسی (تریگر)

تریگر: **«… رو انگلیسی کن»**. در این حالت **از روتر انتشار استفاده نکن**؛
`.dentcast/workflows/en-version.md` را بخوان و طبق آن عمل کن. آن ورکفلو یک صفحهٔ
موجود را در `/{type}/en/{same-filename}.html` آینه می‌کند: chrome انگلیسی از
`metanotes/en/meta-1.html`، بدنه/اسکیمای ارث‌بری‌شده از نوعِ خودِ صفحهٔ منبع، و
تاگل واقعیِ fa↔en روی هر دو طرف. صفحه‌های en ترجمهٔ مستقل‌اند: **نه** انتریِ
brain، **نه** خط پالس، **نه** لینک‌دهی تخصصی.

**نکته — روتر انتشار خودش این ورکفلو را صدا می‌زند.** هر publish عادی روی `.org`
با **فاز D** ورکفلوی انتشار تمام می‌شود که همین ورکفلو را روی صفحهٔ تازه‌منتشرشده
اجرا می‌کند؛ پس صفحهٔ جدید **همیشه** با آینهٔ انگلیسی + تاگل fa↔en منتشر می‌شود
(Hard Rule 12). تریگر «… رو انگلیسی کن» برای صفحه‌های **قدیمی‌تر** است که آن
قاعده را ندیده‌اند. **تنها استثنا LiteCast است:** فقط `.ir`، بدون hreflang، بدون
آینهٔ en و بدون تاگل — در هر دو مسیر.

---

## ۳. پروتکل انتشار تبلیغ (تریگر)

تریگر: **«تبلیغ دارم»**، «آگهی دارم»، «اسپانسر گرفتیم»، «تبلیغ X رو خاموش کن»،
«سهم اسپانسر رو کم/زیاد کن». در این حالت **از روتر انتشار استفاده نکن**؛
`.dentcast/workflows/ads.md` را بخوان و طبق آن عمل کن. تبلیغ محتوا نیست: **نه**
صفحه، **نه** انتریِ brain، **نه** خط پالس، **نه** آینهٔ en — کل سیستم در
`spot/spot-config.json` است (مستندات: `spot/README.md`، قیمت‌گذاری:
`spot/PRICING.md`). آن ورکفلو خلأها را می‌پرسد (اسپانسر یا داخلی، لینک، کپی،
عکس، جایگاه‌ها، سهم چرخش) و اسپانسرها را طوری ثبت می‌کند که خودکار با
`rel="sponsored"` رندر شوند (الزام گوگل). **هرگز تبلیغ را داخل صفحه هاردکد نکن.**

**همیشه بپرس این تبلیغ کدام‌یک از چهار «زمان» چرخش را می‌گیرد و بقیه چه پر
می‌شود** — چرخش هر **بازدیدِ صفحهٔ تبلیغ‌دار** یک قدم جلو می‌رود، پس
`rotation.sequence` چهارتایی = چهار بازدید (چهار ریفرش یا چهار مقاله = یک دور
کامل)، و تبلیغی که به هیچ زمانی تخصیص نیافته **هرگز** رندر نمی‌شود
(`ads.md` قاعدهٔ ۶ + سوال ۶ فاز B).

---

## ۴. پروتکل گزارش تبلیغ (تریگر)

تریگر: «گزارش تبلیغ بده»، «آمار تبلیغات»، «تبلیغ‌ها ماه گذشته چقدر دیده شدن؟»،
«کلیک اسپانسر X چقدر بوده؟» → `.dentcast/workflows/spot-report.md`. این مسیر
**فقط خواندنی** است: نه ویرایش config، نه publish. هر رندر/کلیک هم به **API
خودمان** گزارش می‌شود (`spot_impression`/`spot_click` → شمارنده‌های تجمیعی، خواندن
با `GET /admin/spot/stats`) و هم به GA4 به‌عنوان کنترل متقابل. سه قاعده‌ای که
درستیِ گزارش را تعیین می‌کنند: عدد سرتیتر همیشه **تعداد بارِ نمایش** است (یک نفر
که ۲۰ بار می‌بیند = ۲۰؛ عمداً attribution کاربری وجود ندارد پس «چند نفر»
بی‌پاسخ است)، **پریمیوم صفر است چون طراحی این‌طور است نه چون اندازه‌گیری شده**، و
**هیچ داده‌ای قبل از 2026-07-26 وجود ندارد**. عدد حدس زده نمی‌شود — اگر داده در
دست نیست، دستورش تحویل داده می‌شود.

---

## ۵. پروتکل فایل مقالهٔ پیوست (تریگر — هر نوع، فایل‌محور)

اکشن‌های پیپر با **خودِ فایل** تریگر می‌شوند، نه با نوع محتوا. هر وقت کاربر یک
فایل مقاله/پیپر می‌دهد — با محتوای **هر** نوع (نه فقط DentAI)، یا **بدون** هیچ
متنی — شاخهٔ پیپر فعال می‌شود. جزئیات در **گام ۴.۱۰** فایل
`.dentcast/workflows/README.md` (روتینگ در **فاز ۰**، مسلح‌شدن در **سوال ۴.۷**):

1. **بایگانی در Google Drive** — آپلود در زیرپوشهٔ موضوعیِ درست (انتخاب معنایی)
   از پوشهٔ کابینت:
   `https://drive.google.com/drive/folders/1iDwq4Uj-y7_FO99-QW1Th0hVRN5yfk9f`
2. **غنی‌سازی `dentcast_cabinet_full_catalog.json`** (ریشهٔ ریپو) — افزودن پیپر
   به آرایهٔ `papers` با لینک Drive، هشتگ‌های معنایی (به‌علاوهٔ نام خود مقاله)، و
   اسکیمای enriched.
3. **نویسندهٔ اول ← کردیت DOI** — DOI مقاله را از وب پیدا کن، بعد **نویسندهٔ
   اول** را زیر مطلب با لینک به DOI کردیت بده (سبک ShareHub).

**روتینگ بر اساس چیزی که پیوست شده:**
- **فایل پیپر + متن (هر نوع):** صفحه به‌صورت عادیِ نوعِ خودش منتشر می‌شود **و**
  هر سه اکشن اجرا می‌شوند.
- **فقط فایل پیپر، بدون متن:** **فقط** اکشن ۱ و ۲ (آپلود Drive + به‌روزرسانی
  کاتالوگ) — اکشن ۳ اسکیپ (صفحه‌ای برای کردیت نیست) و بقیهٔ فلوی انتشار هم اسکیپ.
- **متن، بدون فایل پیپر:** هیچ اکشن پیپری؛ انتشار عادی.

گاردِ سختِ این تریگر: **«اگر جایی شک داشتی سوال کن عمل نکن»** — هر جا مطمئن
نیستی (زیرپوشه، موضوع/تگ، DOI، نویسندهٔ اول) **اول بپرس، هرگز حدس نزن.**

---

## ۶. قراردادهای ریپو

- `dentcast-brain.json` — فایل دادهٔ مرکزی؛ یک **آرایهٔ تختِ واحد** از همهٔ
  انتری‌هاست (بدون بخش‌بندی per-type). انتری جدید به انتهای کل آرایه می‌رود؛
  اما **اسکیما** از آخرین انتریِ هم‌دسته گرفته می‌شود.
- `dentcast-hashtag-reference.json` (ریشهٔ ریپو) — **کتابخانهٔ کانونیکالِ
  هشتگ‌ها و تنها منبع حقیقت برای هر `#hashtag`** (جست‌وجوی دستیار هوش مصنوعی از
  آن می‌خواند؛ با `tools/build_plus_index.mjs` در `plus/content-index.json` آینه
  می‌شود). مرجع مالکِ *واژگان* است (کدام فرمِ کانونیکال مجاز است، هرکدام یعنی
  چه، الیاس‌هایش)؛ brain مالکِ *انتساب* (کدام مقاله کدام تگ را دارد).
  **استاندارد، نه متن آزاد:** در هر publish هر هشتگِ پیشنهادی را در برابرِ این
  کتابخانه resolve کن و فرمِ کانونیکالِ موجود را دوباره استفاده کن، یا اگر واقعاً
  نبود یک conceptِ کانونیکالِ نو (با `definition` + `use_when` + `aliases`) بساز
  — الیاس هرگز به‌جای تگ، تگِ کانونیکال هرگز به‌عنوان الیاس، و هیچ concept
  تکراری. ابزار: `tools/hashtag_ref.py` (`--simulate`/`--apply`/`--sync`/`--check`)؛
  batchها در `.dentcast/hashtag-batches/`؛ طبقِ **Hard Rule 15 / گام ۵.۰**ِ
  ورکفلوی انتشار (`.dentcast/workflows/README.md`)، seed‌شده با
  `.dentcast/hashtag-campaign-handoff.md`.
- `dentcast_cabinet_full_catalog.json` — کاتالوگ ریشهٔ کابینت مقالات (آرایهٔ
  `papers`؛ آینهٔ زیرپوشه‌های موضوعیِ Google Drive). با شاخهٔ پیپر (گام ۴.۱۰)
  به‌روز می‌شود؛ رابط جست‌وجو: `dentcast_cabinet_search.html`. تگ‌های
  کباب‌استایلِ `tags`ِ آن یک سیستمِ **جدا** از `#hashtag`های brain بالاست و
  تابعِ کتابخانهٔ مرجعِ هشتگ **نیست**. سه چیزی که کابینت حالا دارد و هیچ
  انتشاری نباید خرابشان کند: **از دسترسِ موتورهای جستجو بیرون است**
  (`robots.txt` هم کاتالوگ و هم همزادِ `.enriched.json` را Disallow می‌کند،
  `_headers` هدرِ `X-Robots-Tag: noindex` می‌دهد، و خودِ صفحه
  `<meta robots noindex>` دارد) — این را «درست» نکن و هرگز واردِ sitemap نکن؛
  روی سایت **صفحه برای همه باز است** («کتابخانهٔ دنت‌کست»، کارت دومِ بخش «موتور
  جستجوی دندانپزشکی»ِ تب آرشیو و ابزارِ «کتابخانه» در همبرگری) و فقط **فایل‌ها**
  پریمیوم‌اند — `#card-library` دیگر روی تپ گیت نمی‌کند و مرورگر نسخهٔ
  بدون‌لینکِ `plus/library-index.json` را می‌گیرد، پس **بعد از گام ۴.۱۰ حتماً
  `python3 tools/build_library_index.py` را دوباره اجرا کن** وگرنه پیپر تازه
  اصلاً روی صفحه ظاهر نمی‌شود؛ و کارت یک **شمارِ دستیِ مقالات**
  را تبلیغ می‌کنند («بیش از ۲۲۰۰ مقاله») که گام ۴.۱۰ با عبور از هر صدِ رُند،
  روی هر دو سطح با هم به‌روزش می‌کند.
- `tools/` — اسکریپت‌های پایتون/نود، از جمله بیلدر اصلی ایندکس.
- **فهرست موضوعی یک URL است با دو چیدمان** (`/pillar/` و `/pillar/<slug>/`).
  `tools/build_pillar.py all` برای هر پیلارِ ساختارمند **دو** فایل می‌نویسد: خودِ
  **صفحه**، که همهٔ مطالبِ آن پیلار را تخت و از تازه‌ترین فهرست می‌کند (بی‌تاریخ‌ها
  ته لیست) و همان چیزی است که هر بازدیدکننده و هر خزنده می‌گیرد؛ و
  **`structure.json`** کنارش، که زیرموضوع‌بندی و ترتیبِ خواندن را نگه می‌دارد.
  `/pillar/premium-index.js` این sidecar را می‌گیرد و روی **همان** URL جایگزین
  می‌کند، فقط بعد از اینکه `/me` اشتراک پریمیوم را تأیید کند — نه صفحهٔ دوم، نه
  ریدایرکت، نه noindex. **هرگز کارت‌های زیرموضوعی را دستی به صفحهٔ پیلار برنگردان
  و هرگز `structure.json` را دستی ویرایش نکن**: هر دو چیدمان در یک سند یعنی همان
  لینک‌ها دو بار روی صفحه، که دقیقاً همان مشکلِ محتوای تکراری است که این تفکیک
  برای پرهیز از آن ساخته شده. سه پیامدِ ماندنی: تاریخ از `datePublished` داخل
  JSON-LD خودِ هر صفحهٔ مقصد می‌آید (هیچ جای دیگری ندارد) و **در زمان build** به
  تقویم شمسی تبدیل می‌شود، چون خزنده باید تاریخ را بخواند نه محاسبه کند؛
  `tools/build_plus_index.mjs` تاکسونومیِ زیرموضوع را از همین sidecarها می‌خواند،
  پس باید **بعد از** بیلدر پیلار اجرا شود؛ و `robots.txt` این sidecarها را
  Disallow می‌کند، که برای لایهٔ پریمیوم هیچ هزینه‌ای ندارد چون قواعد robots هرگز
  شاملِ fetch خودِ صفحه نمی‌شوند.
- **مُهرِ نسخهٔ کش.** فایل‌های مشترک با آدرسِ مُهرخورده لود می‌شوند
  (`/dc-nav.js?v=40`، `/plus/js/pricing-page.js?v=2`)؛ مسیر هیچ‌وقت عوض نمی‌شود،
  پس تنها چیزی که مرورگرِ بازدیدکنندهٔ برگشته را وادار به گرفتنِ فایلِ تازه
  می‌کند همین مُهر است. بعد از تغییرِ **هر** فایلِ مشترک — از جمله ماژولی که
  یک ورودیِ مُهرخورده فقط *import*ش می‌کند، مثل `plus/js/premium-cta.js` — در
  همان کامیت `python3 tools/asset_version.py --bump` را اجرا کن. کلِ گرافِ
  import هر فایل را هش می‌کند، نسخه‌های کهنه را بالا می‌برد، همهٔ صفحه‌ها را
  بازنویسی می‌کند و `.dentcast/asset-versions.json` را به‌روز می‌کند. CI با
  `--check` بررسی می‌کند.
- `index.html` — صفحهٔ اصلی با بخش Pulse و ویجت آخرین محتوا.
- هر نوع محتوا دایرکتوری خودش را در ریشه دارد (`/notecast/`، `/insight/`،
  `/litecast/`، …). مسیر دقیق را از URLهای داخل انتری‌های brain تأیید کن.
- `.dentcast/workflows/` — ورکفلوهای انتشار (+ `agent-parity.md`).
- `spot/` — سیستم تبلیغاتِ config-محور («Spot»؛ نام عمداً خنثی است — قاعدهٔ
  عمومیِ `##.dc-ad` در EasyList نام‌گذاری قدیمیِ `/ads/` + `dc-ad` را زیر هر
  adblocker پنهان می‌کرد، پس **هرگز** از «ad/ads» در نام فایل، مسیر، کلاس، id یا
  data attribute این سیستم استفاده نکن). `spot/spot-config.json` تنها منبع حقیقت
  است (کلید اصلی + هر جایگاه، creativeهای premium/sponsor، `rotation.sequence`،
  targeting با `slots`/`audience`)؛ `spot/spot.js` را یک loader hook در انتهای
  `dc-nav.js` روی همهٔ صفحه‌ها تزریق می‌کند. کاربر ناشناس و Plus رایگان تبلیغ
  می‌بینند؛ پریمیوم نه؛ هر چیزِ خاموش هیچ ردی نمی‌گذارد. **هیچ صفحه‌ای مارک‌آپ
  تبلیغ ندارد.**

---

## ۷. سطوح Plus و Premium (چه چیزی با هر publish باید به‌روز شود)

این بخش دقیقاً همان چیزی است که یک publishِ «عادی» را به یک publishِ کاملِ
Plus/Premium تبدیل می‌کند. همهٔ این‌ها گام‌های خودِ ورکفلوی انتشارند — اینجا فقط
نقشه‌شان است:

| سطح | چه چیزی | کجا | چه زمانی به‌روز می‌شود |
|---|---|---|---|
| عادی | صفحهٔ محتوا + brain + پالس + پیلار | `/{type}/`، `dentcast-brain.json`، `index.html` | هر publish |
| عادی | آینهٔ انگلیسی + تاگل | `/{type}/en/` | فاز D — هر publish روی `.org` (جز LiteCast) |
| Plus | درخت ناوبریِ «میز کار» | `plus/content-index.json` | `node tools/build_plus_index.mjs` — هر publish که brain را تغییر داده |
| Plus/Premium | فلش‌کارت (Leitner) | `DefinedTermSet` روی خودِ صفحه → `plus/flashcards-index.json` | گام ۴.۱۱ + `node tools/build_flashcards_index.mjs` |
| Plus/Premium | کوییز نمره‌دار | `FAQPage` روی خودِ صفحه → `plus/quiz-index.json` | گام ۴.۱۲ + `node tools/build_quiz_index.mjs` |
| Premium | مسیرهای یادگیری (pathways) | `plus/pathways.json` | گام ۵.۶ — هر publish تخصصی (جز LiteCast) |
| Premium | کالکشن‌ها | دیتابیس API (`collections`/`collection_items`) | کاربر می‌سازد؛ publish کاری ندارد |
| Premium | دفترچهٔ هایلایت‌ها | دیتابیس API (`highlights`) → `/plus/highlights.html` | کاربر می‌سازد؛ publish کاری ندارد |
| Premium | کتابخانهٔ کابینت | `dentcast_cabinet_full_catalog.json` → `dentcast_cabinet_search.html` | فقط **گام ۴.۱۰** (پیپر پیوست‌شده) — publishِ بی‌پیپر کاری با آن ندارد |
| Plus/Premium | نوتیف مطلب جدید | `<meta name="dc-notify" content="true">` | فاز E — جز paper-only/LiteCast/glossary |

جزئیات معماری:

- `plus/pathways.json` — مسیرهای یادگیری DentCast Plus (اسکیمای §۵ اسپک؛ فاز ۳
  پریمیوم). یک pathway یک **سفر یادگیری منتخب** است، نه نمای pillar: برخلاف
  pillar (هر آیتم یک خانه)، **یک آیتم می‌تواند در چند pathway باشد**، هر بار در
  جای درستِ پیش‌نیاز→پیشرفته. هر publish تخصصی در **گام ۵.۶** محتوای جدید را به
  pathwayهای مربوطه تخصیص می‌دهد (تصمیم **معنایی**؛ اگر شک داشتی بپرس). ابزارها:
  `tools/pathway_place.py` (پیشنهاد جایگذاری + `--insert` + `--coverage`) و
  `tools/pathway_scout.py` (جست‌وجوی کاندید + `--steps` + `--coverage`).
  کاتالوگ/داکترین: `reports/pathways-catalog-2026-07-22.md`. بک‌اند فعال است —
  `plus-api/src/pathways.ts` آن را می‌خواند (کش‌شده، reload-on-change مثل
  `content-index.ts`) و `GET/POST /pathways*` (پریمیوم‌گیت) و `active_pathway` در
  `GET /me` از همان می‌خوانند؛ `user_pathways` ثبت‌نام + کشِ مشتقِ
  `current_step`/`completed_at` را نگه می‌دارد (هرگز با فراخوانی کلاینت جلو
  نمی‌رود — در هر خواندن از highlightها/`user_activity` بازمحاسبه می‌شود).
  ویرایش این فایل دیگر redeploy API نمی‌خواهد: نسخهٔ بیک‌شده در `PATHWAYS_PATH`
  فقط مقدار بوت است و `plus-api/src/content-refresh.ts` هر چند دقیقه فایل
  منتشرشدهٔ `plus/pathways.json` (و `plus/content-index.json`) را از خود سایت
  دوباره می‌گیرد — با اعتبارسنجی، فقط رو به جلو، و در هر شکستی آخرین نسخهٔ سالم
  می‌ماند. فرانت هم فعال است:
  `/plus/pathways.html` (کاتالوگ)، `/plus/pathway.html?id=` (فهرست گام‌ها +
  ثبت‌نام)، و بلاک «مسیر یادگیری» در داشبورد `/plus/`
  (`plus/js/pathways.js` رندرکنندهٔ مشترک).
- `plus/quota.json` — **سهمیهٔ رایگانِ هر قابلیت پریمیوم**، و دلیلِ اینکه هیچ‌کدامشان دیگر درِ قفل نیستند. همان ماشینِ `badges.json`/`pathways.json`: در ریپو نسخه‌بندی می‌شود، `plus-api/src/quota.ts` می‌خواندش، و `content-refresh.ts` زنده دوباره می‌گیردش — پس **تنظیم دوبارهٔ یک سقف یک کامیت به سایت است نه deploy API**، و همین کل نکته است، چون عددِ سهمیه یک قیمت است و قیمت را قیف بارها جواب می‌دهد. اعمال در `plus-api/src/services/quota.ts`. پنج قاعده‌ای که طراحی رویشان بنا شده: **هر شمارنده مشتق می‌شود، هرگز ذخیره نمی‌شود** — کارت‌های مرور از `user_activity`، کیس‌های دستیار از `assistant_rounds`، بردها از `collections` — پس این سیستم **اصلاً migration لازم نداشت**، چیزی برای backfill ندارد، و نمی‌تواند از لاگِ مبدأش فاصله بگیرد (همان دکترینِ دیوار افتخارات)؛ **مرز همیشه آنجاست که داده تولید می‌شود، نه آنجا که کشیده می‌شود** — دکمهٔ مخفی سهمیه نیست، پس هر route یا رد می‌کند (402 `quota_exhausted`) یا payload خودش را قبل از خروج از پروسه می‌بُرد، و `plus/js/quota.js` فقط شمارنده و متن را می‌کشد؛ **دو نوع دیوار، عمداً با دو لحن** — سهمیهٔ تمام‌شده پُر می‌شود و می‌گوید کِی (`resets_at`)، ولی مرزِ preview (`premium_required`: مسیرهای قفل، نقشهٔ نقطه‌کورِ قطب‌نما) هرگز پُر نمی‌شود و نباید وانمود کند؛ **چکِ مصرفی زیر قفل گرفته می‌شود** (`pg_advisory_xact_lock` در `consumeQuota`، در همان تراکنشی که ردیفِ شمرده‌شده را می‌نویسد) چون check-then-consume یک دور زدنِ واقعی است نه نظری؛ و **دستیار «کیس» را حساب می‌کند نه «راند»** — هویت، hashِ سمت سرورِ خودِ توصیف است (`caseRunHash`)، پس history جعلی از سقف رد نمی‌شود و ادامهٔ کیسِ پذیرفته‌شده همیشه رایگان است. چه چیزی متر **نمی‌شود**: سه مزیتِ پیوسته (بدون تبلیغ، اطلاع فوریِ مطلب تازه، ضریب امتیاز) که اصلاً چیزی برای چشیدن ندارند. تست‌ها: `plus-api/test/quota.test.ts` به‌علاوهٔ بلوکِ free-tier در سوئیتِ خودِ هر قابلیت.
- **مرزِ کتابخانهٔ مقالات خودِ «مقاله» است، نه «جستجو»** (`plus/library-index.json`، `plus-api/src/library.ts`، `routes/library.ts`، کلیدِ سهمیهٔ `library_papers`). جستجو، فیلتر و مرتب‌سازی روی کل ۲۲۰۰ مقاله برای همه رایگان است — چه لاگین باشند چه نه؛ آنچه پریمیوم می‌خرد باز کردنِ فایل است. این طراحیِ دوم است: اولی **جستجو** را با شمارندهٔ localStorage متر می‌کرد، که از دو جهت مرزِ غلطی بود — قابل اعمال نبود (شمارنده روی دستگاهِ خودِ خواننده) و چیزِ اشتباهی می‌فروخت (توانِ نگاه کردن، نه چیزی که دنبالش می‌گردند). سه چیز مرزِ تازه را واقعی می‌کند: صفحه `plus/library-index.json` می‌گیرد (ساختهٔ `tools/build_library_index.py` — عنوان/تگ/id و **هیچ فیلدِ `drive_*`**، ۷۲۱ کیلوبایت به‌جای ۶ مگابایتِ کاتالوگ اصلی که قبلاً کامل دانلود می‌شد)، پس هیچ لینک Drive ای اصلاً در payload نیست؛ `GET /library/paper/:id` تنها تولیدکنندهٔ لینک است و اول اشتراک را چک می‌کند؛ و هر مقاله در هر دوره **یک بار حساب می‌شود، هر چند بار که باز شود** (`count(distinct content_id)`)، پس دوباره‌خوانی رایگان است و کسی یاد نمی‌گیرد دانلودها را انبار کند. idِ ناشناخته و idِ بدونِ فایل یک جواب می‌گیرند، تا این route راهی برای شمارشِ کاتالوگ نشود. **آنچه این کار حل نمی‌کند و هرگز نباید طورِ دیگری توصیف شود:** کاتالوگ اصلی فایلی در یک ریپوی **public** است و تاریخچهٔ گیت همهٔ نسخه‌هایش را نگه می‌دارد، پس لینک‌هایی که قبلاً منتشر شده‌اند همچنان پیدا می‌شوند، و فایلی که در Drive با لینک به اشتراک گذاشته شده برای هر کسی که لینک را دارد باز می‌ماند. راهِ ماندگار سمتِ Drive است — لغو اشتراک‌گذاری با لینک و ساختنِ URLهای کوتاه‌عمر با service account — و دقیقاً در `resolvePaper()` جا می‌گیرد.
- `plus/badges.json` — کاتالوگ نشان‌های بخش «افتخارات» پروفایل (۲ مدال لیگ + ۲۳ نشان). همان ماشینِ `pathways.json`: در ریپو نسخه‌بندی می‌شود، `plus-api/src/badges.ts` می‌خواندش (reload-on-change، نسخهٔ منتشرشده برنده، در هر شکستی آخرین نسخهٔ سالم)، و `content-refresh.ts` زنده دوباره می‌گیردش — پس **تغییر یک آستانه یا بازنویسی متنِ فارسیِ یک نشان، یک کامیت به سایت است نه deploy API**. سرو با `GET /achievements` (`plus-api/src/routes/achievements.ts`)، رندر با `plus/js/achievements.js` داخل `profile.js` بین «رکوردها» و «لیگ من». چهار قاعده‌ای که کد رویشان بنا شده: **هر نشان مشتق می‌شود، هرگز ذخیره نمی‌شود** (`plus-api/src/services/achievements.ts` هر ۲۳ معیار را از دادهٔ موجود حساب می‌کند) — همین است که کل قفسه را گذشته‌نگر می‌کند، بدون ستون `earned_at` که بتواند با لاگِ مبدأش اختلاف پیدا کند و بدون backfill در روز انتشار؛ **یک مقایسه و فقط یکی، `value >= threshold`**، پس هر معیارِ ذاتاً برعکس (رتبهٔ ثبت‌نام در «پیشگام») در سرویس به ۰/۱ تبدیل می‌شود نه اینکه ارزیابی مسیر دوم پیدا کند؛ **سطح یک رنگِ حلقه است، نه یک کلمه** — دیوار هرگز «طلا» نمی‌نویسد، فقط ردیفِ مدال می‌نویسد و آن‌جا هم هرگز تنها نمی‌آید («طلای کامپوزیت»)، چون دو مقیاسِ فلزی در این بخش کنار هم‌اند (هفت مادهٔ دندانیِ لیگ و برنز/نقره/طلای نشان‌ها) و کلمه همان چیزی‌ست که تصادف می‌کند؛ و **هر کوئریِ متکی به رتبهٔ لیگ فیلترِ اعتبار را دارد** (اندازهٔ گروه `>= min_valid_group_size` **و** `weekly_xp > 0`) چون `league-finalize.ts` برای همهٔ اعضای همهٔ گروه‌ها `final_rank` می‌نویسد، حتی گروه‌هایی که خود UI غیررقابتی اعلامشان می‌کند — بدون این فیلتر، اولین مدالِ بیشترِ حساب‌ها بابتِ هیچ صادر می‌شود. `visibility` تعیین می‌کند نشانِ کسب‌نشده چه شکلی‌ست: `always` (خاکستری، با شرطش)، `mystery` (کاشیِ «؟» که هیچ نشتی ندارد — سرور نام و آیکون و متن را حذف می‌کند)، `earned_only` (اصلاً نیست، برای شرطی که دیگر برای کسی قابل‌کسب نیست). تست‌ها: `plus-api/test/achievements.test.ts` + `achievements.dom.test.ts`.
- **اطلاعیه — صندوقِ درون‌سایتی** (`plus/js/notices.js`، `plus-api/src/services/notices.ts`). هر اعلانی که API می‌سازد از قبل دقیقاً از **یک** تابع رد می‌شد — `sendCapped` در `notify-policy.ts`، «تنها دری که هر اعلان خروجی از آن رد می‌شود» — پس اطلاعیه یعنی همان در، متنِ پیام را هم در همان ردیفِ `notification_log` که از قبل می‌شمرد **بنویسد**. هفت نوع اعلان بدون دست‌خوردنِ هیچ‌کدام از سرویس‌هایشان صاحب خانه شدند. دو چیز را درست می‌کند که به‌شکل باگ دیده نمی‌شدند: کاربری که اجازهٔ push نداده و پیام‌رسان وصل نکرده **هیچ‌چیز** نمی‌گرفت (providerها برای کاربرِ بی‌مقصد بی‌صدا no-op می‌کنند)، و سقفِ روزانه اضافی را **دور می‌ریخت** نه اینکه صف کند، پس روزِ شلوغ حتی برای کسی که کانال داشت خبر گم می‌کرد. حالا سقف فقط تصمیم می‌گیرد پیام **سفر کند** یا نه؛ همیشه می‌رسد. **تنها قاعده‌ای که کل این بنا رویش ایستاده: ستون `delivered` دو شغلِ جدول را از هم جدا می‌کند و هر کوئری‌ای که می‌پرسد «آیا فرستاده شد» آن را دارد** (`sentCountOn`، `alreadySentKindOn`) — جدول شمارندهٔ سقف هم هست، پس شمردنِ ردیف‌های صرفاً درون‌سایتی یعنی سقف خودش را تغذیه کند و بی‌صدا بودجهٔ push کاربر را ببندد. وضعیت خوانده‌شدن **یک watermark** است (`profiles.notices_seen_at`) نه پرچمِ per-row، پس حالتِ نیمه‌خوانده وجود ندارد و retry بی‌اثر است. نشانگرش یک **نقطه روی آیکون حساب** است، هرگز تغییرِ رنگِ خودِ آیکون: آدمک نشانگرِ هویت است (خاکستری = مهمان، آبی = وارد شده) و قرمزکردنش یعنی کاربر دیگر نفهمد وارد شده یا نه، و در قراردادِ رابط‌ها یعنی «مشکلی در حسابت هست». داخل همان میزبانِ `overlay.js` رندر می‌شود (مثل پیشخوان و پروفایل)، نه یک صفحهٔ تازه. **پخش همگانی** (`notice_broadcasts`، migration 0026) نیمهٔ دوم است: یک ردیف برای همه به‌جای fan-out، و وضعیت خوانده‌شدنِ هر کاربر مجانی از همان watermark می‌آید. دو چیز از آن استفاده می‌کنند. **انتشار هر مطلب همان لحظه، در هر ساعتی، برای همهٔ کاربران در اطلاعیه می‌نشیند** — پنجرهٔ بیداری و سقف روزانه برای محافظت از یک گوشی و یک توکنِ بات‌اند و هیچ‌کدام در نوشتنِ یک ردیف دخالتی ندارند، پس کاربر رایگان دیگر ۲۴ ساعت منتظر دایجست نمی‌ماند و انتشارِ ساعت ۲ بامداد ساعت ۲ بامداد در صندوق است. متنِ اطلاعیه عمداً **کوتاه** است و پالس نیست («مطلب جدید در {بخش}» + عنوان مقاله): پاراگرافِ توضیحِ مقاله جای lock screen است، نه ردیفی که کاربر اسکن می‌کند. پوش‌هایی که بعدش می‌آیند `inbox: false` می‌دهند تا یک انتشار دو بار دیده نشود — ردیفِ شمارنده‌شان (بدون عنوان) همچنان نوشته می‌شود، چون همان ردیف **خودِ شمارندهٔ سقف** است. و **`POST /admin/notices/broadcast`** اطلاعیهٔ شخصیِ بنیان‌گذار است، با فرمی روی همان صفحهٔ `GET /admin`: ردیفِ اطلاعیه همیشه بلافاصله می‌نشیند، ولی `push` مگر با `force` پنجرهٔ بیداری را رعایت می‌کند — «بی‌سقف» یعنی یک broadcast بودجهٔ کاربر را نخورد، نه مجوزِ بیدارکردنِ مردم در ۳ بامداد. دو قاعده مخاطبِ یک broadcast را می‌بندد: `audience` (همه/رایگان/پریمیوم، در لحظهٔ خواندن) و `created_at > profiles.created_at`، تا هیچ‌کس خبری از قبل از وجودِ خودش نبیند؛ `profiles.notices_seen_at` هم به همین دلیل default آن `now()` است. تست‌ها: `plus-api/test/notices.test.ts` + `notices.dom.test.ts`.
- **اعلامِ نشانی که هرگز ذخیره نشده** (`plus-api/src/services/achievement-sync.ts`، `achievement_announcements`). چون هر نشان مشتق می‌شود (بالا، `badges.json`)، هیچ کدی نمی‌تواند بفهمد نشانی *همین‌الان* کسب شد — پس چیزی که ذخیره می‌شود عمداً **خودِ نشان نیست، اعلام است**: دفترِ آنچه تا حالا به این کاربر گفته‌ایم. سه قاعده نمی‌گذارد این همان منبعِ دومِ حقیقتی شود که طراحیِ مشتق ردش کرد: دیوار هرگز از آن نمی‌خواند (`GET /achievements` دست‌نخورده)؛ **high-water mark** است، پس نشانی که خاموش و دوباره روشن می‌شود — «فاتح» با هر انتشارِ تازه که پوشهٔ کامل را زیر ۱۰۰٪ می‌برد — دوباره اعلام نمی‌شود؛ و **هیچ‌چیز عطف به ماسبق نیست**. این آخری کلِ داستانِ روزِ انتشار است و در `profiles.achievements_seeded` زندگی می‌کند: migration 0025 آن را برای هر حسابی که آن روز وجود داشت FALSE می‌کند و DEFAULT را TRUE می‌گذارد — حسابِ قدیمی دقیقاً یک بار به‌صورت خاموش جبران می‌شود (تاریخچه‌اش بایگانی و از پیش تأییدشده، بدون هیچ اطلاعیه‌ای) و حسابی که بعد از آن ساخته می‌شود با دفترِ خالی شروع می‌کند و **اولین نشانش به او گفته می‌شود**. sync با debounce پنج‌دقیقه‌ای و fire-and-forget از مسیرهای نوشتن اجرا می‌شود، per-user ادغام می‌شود، و drain دارد (`drainAchievementSyncs`) تا `truncate` تست‌ها با آن deadlock نکند. کارتِ جشن روی یک **لیستِ سفید** از سطوحِ آرام باز می‌شود — پروفایل و پیشخوان، هرگز صفحهٔ مقاله — چون روی پوستهٔ دسکتاپ مقاله اصلاً صفحه نیست (`index.html` آن را fetch و داخل ستون C تزریق می‌کند) و کاربرِ وسطِ پاراگراف از نظر URL روی صفحهٔ اصلی است، پس لیستِ سیاه دقیقاً در بدترین لحظه صفحه‌اش را می‌پوشاند. تأییدش از watermarkِ اطلاعیه جداست (`POST /achievements/seen`)، پس خواندنِ اطلاعیه هرگز یک جشنِ نمایش‌داده‌نشده را خرج نمی‌کند.
- `plus/js/sheet.js` — باتم‌شیتِ مشترک. اول داخل `collections.js` برای انتخابگرِ بورد نوشته شده بود و وقتی دیوارِ افتخارات همان شیء را خواست به این‌جا منتقل شد؛ به‌جای پیاده‌سازی دوبارهٔ یک شیت، به این ماژول اضافه کن (همان قاعدهٔ `hl-view.js`).
- **دفترچهٔ هایلایت‌ها** (`/plus/highlights.html`، پریمیوم). تنها جایی که کاربر
  **همهٔ** هایلایت‌هایش را یکجا، گروه‌بندی‌شده بر اساس مقاله و همراه یادداشت‌ها
  مرور می‌کند — بدون رفتن به مقاله و بدون «میز کار». دلیل وجودش: پیش از آن
  هایلایت‌ها عملاً یک‌طرفه بودند — داشبورد شش ردیف نشان می‌داد، هر ردیف لینکی به
  مقاله بود، و صفحهٔ مقاله تا وقتی دوباره «میز کار» زده نشود هیچ نشانه‌ای از
  هایلایت‌های کاربر نمی‌کشد (گزارش کاربر، ۲۰۲۶-۰۸-۰۵). بک‌اند:
  `GET /highlights/library` (`requirePremium`) در
  `plus-api/src/routes/highlights.ts`؛ `GET /highlights/recent` هم
  `total`/`article_count` کلِ کتابخانه را در هر پلنی برمی‌گرداند. فرانت:
  `plus/js/highlights.js` (رندرکننده) + `plus/js/highlights-page.js` (گیت). دو
  ثابت: **هر هایلایت یک کارت است، نه لینک** (متن کامل + یادداشت، محتوای همان
  ردیف است؛ «متنِ مقاله» فقط یکی از کنش‌های کوچک کنارش)، و **هر لینک به مقاله
  `?dcphl=<highlight_id>` دارد** — آن‌وقت `plus.js` میز کار را باز می‌کند و
  `Workbench.focusHighlight()` روی همان mark اسکرول می‌کند و لحظه‌ای می‌درخشاندش.
  فهرست اخیرِ داشبورد و pinهای هایلایتِ کالکشن هم با همین لینک می‌روند. دو ثابت
  دیگر: **یادداشت/برچسب/رنگ همان‌جا ویرایش می‌شوند**
  (`inlineEditor` در `hl-view.js` → `PATCH /highlights/:id`)، و **همهٔ فیلترها
  در URL می‌نشینند** (`?q&label&color&folder&sort&view` با `replaceState`) تا یک
  نمای فیلترشده با رفرش و دکمهٔ بازگشت از بین نرود. دو نما (گروه‌بندی مقاله /
  فهرست زمانی)، چیپ‌های برچسب با شمارش، فیلترِ رنگ، فهرست پرش بین مقاله‌ها،
  میان‌برِ `/` برای جستجو، کپیِ تکی/مقاله/نتایج، و رندرِ تدریجی با
  `IntersectionObserver` هم دارد. آنچه پریمیوم می‌خرد **نمای تجمیعی** است، نه
  دسترسی به دادهٔ خود کاربر: شش ردیف داشبورد، میز کارِ هر مقاله و
  `GET /export/highlights` روی همهٔ پلن‌ها می‌مانند.
- **`plus/js/hl-view.js`** — واژگانِ مشترکِ هر سطحی که یک هایلایتِ ذخیره‌شده را
  نشان می‌دهد (دفترچه، بردِ کالکشن، و هرچه بعداً بیاید): `hlMark`/`noteBlock`/
  `labelChip`/`actionBtn`، `foldFa` (فولدینگِ فقط-برای-جستجو روی ZWNJ و ی/ک عربی
  — هرگز روی متنِ ذخیره‌شده)، `highlightHref` (لینکِ `?dcphl=`)، `toast`،
  `skeleton`، `confirmStrip` و `inlineEditor`. به‌جای ساختنِ کارتِ تازه، به این
  ماژول اضافه کن.
- **کالکشن‌ها** (فاز ۳ پریمیوم؛ `collections`/`collection_items` اسپک §۴، از
  migration 0001 provision شده، از ایندکس‌های migration 0012 زنده). برخلاف
  pathway (منتخبِ بنیان‌گذار) یا آرشیو موضوعی (گروه‌بندی خودکار با تاکسونومی)،
  کالکشن **کاملاً مال خود کاربر** است — هر ترکیبی از highlightها و صفحه‌های کامل،
  بدون وابستگی به تاکسونومی. بک‌اند: `plus-api/src/routes/collections.ts` (CRUD
  کامل + افزودن idempotent آیتم، همه `requirePremium`). فرانت:
  `/plus/collections.html`، `/plus/collection.html?id=`، بلاک «کالکشن‌ها» در
  داشبورد `/plus/`، و دکمه‌های **«افزودن به کالکشن»** — دکمهٔ دوهدفه در تولبار
  workbench (highlight انتخاب‌شده، یا کل صفحه اگر چیزی انتخاب نشده — همان الگوی
  دکمهٔ یادداشت) و دکمهٔ هر ردیف در فهرست highlightهای اخیرِ داشبورد. هر سه از
  `openCollectionPicker()` در `plus/js/collections.js` می‌گذرند که پیام گیتِ
  رایگان را هم خودش نشان می‌دهد (روی کلیک، نه صفحهٔ قفل). **pin یک کارت است، نه
  لینک** — همان قاعدهٔ دفترچه، با همان محتوا (mark، یادداشت، برچسب) به‌علاوهٔ
  کنش‌های خودش: ویرایشِ درجا، کپی، **انتقال** به بردِ دیگر، «متنِ مقاله ›»
  (`?dcphl=`) و حذف. انتقال اندپوینت نمی‌خواهد: `openCollectionMove()` اول به
  مقصد اضافه می‌کند و بعد حذفِ خودِ برد را صدا می‌زند؛ چون افزودن در سطح دیتابیس
  idempotent است، بدترین حالتِ نیمه‌کاره این است که آیتم در هر دو برد بماند، نه
  اینکه گم شود. کاتالوگ جستجو + مرتب‌سازی دارد و هر برد `item_count` و
  `last_item_at` را نشان می‌دهد که `GET /collections` آن را **مشتق** می‌کند
  (`max(ci.created_at)`) نه ذخیره — بدون ستون، بدون migration. صفحهٔ برد هم
  فیلترِ نوع (هایلایت/صفحه) و متن دارد. هر دو سطح از `hl-view.js` استفاده
  می‌کنند و شیتِ انتخاب (`chooserCard`) یک کامپوننت است برای «افزودن» و
  «انتقال». migration **۰۰۱۸** هویتِ خودِ برد را اضافه می‌کند
  (`emoji`/`color`/`description`، همه اختیاری — بردی که هیچ‌کدام را ندارد دقیقاً
  مثل قبل رندر می‌شود) و `collection_items.position` را: **NULL یعنی «هرگز
  چیده نشده»**، پس بردِ دست‌نخورده هنوز تازه‌ترین-اول باز می‌شود، و
  `PUT /collections/:id/items/order` **کلِ** ترتیب را یکجا می‌نویسد (آرایهٔ خالی
  پاکش می‌کند) — برای همین حالتِ نیمه‌چیده وجود ندارد و تکرارِ درخواست بی‌اثر
  است. «چیدمان» در UI یک **مود** است (`⇅ چیدمانِ دستی`): ماسونری را تک‌ستونه
  می‌کند، فیلترها را کنار می‌گذارد (↑/↓ روی زیرمجموعهٔ فیلترشده آیتم را از روی
  کارت‌هایی که دیده نمی‌شوند رد می‌کند) و به هر pin دکمهٔ ↑/↓ و «۲ از ۷» می‌دهد —
  دکمه، نه drag، چون کشیدنِ کارتِ ماسونری روی موبایل غیرقابل‌استفاده و برای
  کیبورد نامرئی است.

---

## ۸. ثابت‌های سراسری سایت

- **Google Analytics 4 (deferred) روی هر صفحه.** هر صفحهٔ HTML باید اسنیپت
  deferred GA4 (شناسهٔ `G-GMM0WC8X3M`) را داخل `<head>` داشته باشد. lazy-load است
  — `gtag.js` فقط بعد از رویداد `load` اضافه می‌شود تا first paint را بلاک نکند.
  از اسنیپت پیش‌فرضِ async گوگل **استفاده نکن**؛ الگوی زیر را بگذار. چون صفحهٔ
  جدید از آخرین صفحهٔ هم‌دسته کلون می‌شود، تگ خودکار منتقل می‌شود — اما همیشه
  تأیید کن که از کلون جان سالم به در برده.
  - تزریق‌کنندهٔ کانونی: `.github/scripts/inject_ga.py` (idempotent؛ صفحه‌هایی که
    دارند و صفحه‌های بدون `<head>` را رد می‌کند). بک‌فیل/تأیید کل سایت:
    `python3 .github/scripts/inject_ga.py` (یا `--check` برای fail در نبودش).
  - تمپلیت‌های بیلد هم همین را emit می‌کنند: `GA_DEFERRED_SNIPPET` در
    `tools/build_episodes.py` و `tools/build_pillar.py` (و literal هدِ glossary).
    اگر اسنیپت عوض شد، همه را هم‌گام نگه دار.
  - کدبیس‌های `.org` و `.ir` آینهٔ هم‌اند؛ همین تگ عمداً روی هر دو deploy می‌شود.
    منطق per-domain **اضافه نکن**.
  - اسنیپت دقیق (داخل `<head>`):
    ```html
    <!-- Google Analytics (deferred: loads only after the page is fully rendered) -->
    <script>
      window.addEventListener('load', function () {
        window.dataLayer = window.dataLayer || [];
        function gtag(){ dataLayer.push(arguments); }
        window.gtag = gtag;
        gtag('js', new Date());
        gtag('config', 'G-GMM0WC8X3M');
        var ga = document.createElement('script');
        ga.async = true;
        ga.src = 'https://www.googletagmanager.com/gtag/js?id=G-GMM0WC8X3M';
        document.head.appendChild(ga);
      });
    </script>
    ```
  - اگر روزی CSP اضافه شد، باید `script-src https://www.googletagmanager.com` و
    `connect-src https://*.google-analytics.com https://*.analytics.google.com`
    را اجازه بدهد. (فعلاً هیچ CSPای در این ریپو نیست.)

- **preconnect به API روی همهٔ صفحه‌ها.** هر صفحهٔ HTML در ابتدای `<head>` دو
  لینک `preconnect` به `api.dentcast.ir` و `api.dentcast.org` با `crossorigin`
  دارد. تزئینی نیست، مسئلهٔ درستیِ اسپانسر پولی است: کارت Spot تا جواب `/me`
  رندر نمی‌شود (همان قفلی که «پریمیوم تبلیغ نمی‌بیند» را واقعی می‌کند و
  **نباید** شل شود)، پس در صفحهٔ اول هر تب هندشیکِ سردِ DNS+TLS دقیقاً جلوی
  ایمپرشن اسپانسر می‌نشست — اندازه‌گیری‌شده ۲.۷ تا ۳ ثانیه تا اولین کارت، در
  برابر ۷۰ تا ۱۷۰ میلی‌ثانیه در ریفرش.
  - تزریق‌کننده: `.github/scripts/inject_preconnect.py` (idempotent، با
    `--check`). `tools/build_pillar.py` مقدار `PRECONNECT_SNIPPET` را می‌نویسد و
    `tools/episodes_template.html` آن را درون‌خطی دارد.
  - **`crossorigin` الزامی است**: API با credentials صدا زده می‌شود و اتصالِ
    ناشناس برای درخواست CORSِ credentialed بازاستفاده نمی‌شود.
  - هر دو میزبان روی همهٔ صفحه‌ها عمدی‌اند — دو کدبیس آینهٔ هم‌اند و منطق
    per-domain ممنوع است.

- **نماد اعتماد الکترونیکی (enamad) — سه جایگاه، فقط روی dentcast.ir.** نماد
  دقیقاً روی سه سطح است و عنصر per-page **نیست**: فوتر `index.html` (شلِ
  موبایل)، ردیف `.dcd-a-seal` در سایدبار ستون A همان فایل (شلِ دسکتاپ، جایی که
  آن فوتر `display:none` است)، و `.dc-trustseal-box` در `about.html` زیر کارت
  تماس. روی صفحه‌های محتوایی **کلونش نکن** — هر کپی یک ریکوئست به سرور اینماد
  است با ارزش اعتمادسازیِ صفر در صفحهٔ مقاله.
  - **این تنها استثنای عمدیِ قاعدهٔ «منطق per-domain اضافه نکن» است.** نماد برای
    یک دامنه صادر می‌شود (مالِ ما: `dentcast.ir`) و
    `trustseal.enamad.ir/logo.aspx` بر اساس referrerِ درخواست رندر می‌کند؛ پس
    روی میرورِ `.org` تصویرِ *نماد نامعتبر* برمی‌گرداند. دو لایه گیتش می‌کنند:
    یک گاردِ inline در `<head>` هر دو صفحه که خارج از `.ir` کلاس `dc-no-seal` را
    روی `<html>` می‌گذارد (قبل از پارس‌شدن body پنهانش می‌کند، بدون فلش)، و یک
    بلاک در انتهای `dc-nav.js` که نود را کاملاً حذف می‌کند. تصویرها
    `loading="lazy"` هستند و هر دو نماد زیر fold می‌نشینند، پس روی `.org` معمولاً
    اصلاً درخواستی فرستاده نمی‌شود.
  - **اتریبیوت‌هایی که اینماد اعتبارسنجی می‌کند عیناً کپی می‌شوند و هرگز بازنویسی
    نمی‌شوند** — `referrerpolicy='origin'`, `id`, `Code`, و اتریبیوت `code` روی
    `<img>`. فقط `alt`، `loading` و wrapper مالِ ماست. مارک‌آپ **به‌صورت استاتیک
    در HTML** می‌آید (هرگز با JS تزریق نمی‌شود) تا خزندهٔ خودِ اینماد بخواندش.
  - استایل `.dc-trustseal` در `dc-theme.css` است و در `<style>` درون‌خطیِ
    `index.html` آینه شده (index هیچ CSS مشترکی لود نمی‌کند). چیپ در **هر دو** تم
    سفید است، عمداً — آرت‌ورکِ نماد پس‌زمینهٔ روشن فرض می‌کند — و جعبه‌اش
    اندازهٔ ثابت دارد تا با آمدن تصویر چیزی جابه‌جا نشود.
  - ریدرِ شلِ دسکتاپ `.dc-trustseal` را همراه بقیهٔ chrome صفحه strip می‌کند
    (لیست سلکتورهای حذف chrome در `index.html`): `dc-nav.js` از آن سند حذف
    می‌شود، پس نمادِ داخل iframe از گیتِ دامنه فرار می‌کرد.

---

## ۹. رفتار ایجنت (همسانی خروجی)

قواعد رفتاری — زبان گفت‌وگو، گیت‌های تأییدی که هرگز خودکار نمی‌شوند، نقشهٔ
ابزارها وقتی توانمندی‌ای را نداری، دستورهای قطعیِ هش/بیلد/اعتبارسنجی، چک‌لیست
همسانیِ خروجی، و پروتکل گیت — همه در:

**`.dentcast/workflows/agent-parity.md`**

خلاصهٔ سه بندِ حیاتی‌اش:

1. **فایل کانونی را کامل بخوان، هر بار.** خواندنِ ناقصِ `README.md` (بیش از ۱۲۰۰
   خط) و شروع کار، نقض است.
2. **هرگز حدس نزن.** عدد، تاریخ، DOI، نویسنده، عنوان یا هشتگی که کاربر نداده و
   از فایل/تاکسونومی/منبع واقعی قابل استخراج نیست — پرسیده می‌شود.
3. **گزارش نهایی، قالبِ الزامی است.** فهرست «Final output summary» در انتهای هر
   ورکفلو باید کامل بیاید؛ بندی که مصداق ندارد با جملهٔ صریحِ اسکیپ می‌آید، نه
   با حذف.
4. **«فکر می‌کنم همه‌چیز را انجام دادم» گزارش نیست.** publish با **فاز F**
   (`tools/verify_publish.py`) بسته می‌شود، نه با ادعا. تا وقتی خروجی صفر نشده،
   کار تمام نیست.

---

## ۱۰. نگهداری — چه فایل‌هایی با هم به‌روز می‌شوند

| اگر این عوض شد | این‌ها هم در همان کامیت عوض شوند |
|---|---|
| `CLAUDE.md` | `AGENTS.md` + `.cursor/rules/dentcast-router.mdc` |
| ورکفلوی جدید در `.dentcast/workflows/` | جدول روتر در `CLAUDE.md` + `AGENTS.md` + یک `.mdc` اختصاصی در `.cursor/rules/` |
| فهرست/ترتیب بیلدرها | `.dentcast/workflows/agent-parity.md` بخش ۳ |
| گیت‌های تأیید یا Hard Ruleها | `.dentcast/workflows/agent-parity.md` بخش ۱ و ۴ |
| قاعده‌ای که `tools/verify_publish.py` چکش می‌کند | خودِ اسکریپت + فاز F در `README.md` — قاعده و چکش با هم عوض می‌شوند |

**قاعده: منطق در فایل کانونی، مسیر در روترها، رفتار در `agent-parity.md`.**
هیچ‌کدام محتوای دیگری را کپی نمی‌کند.
