# AGENTS.md — DentCast Repo Protocol (agent-neutral)

این فایل نسخهٔ **مستقل از ایجنتِ** `CLAUDE.md` است. هر ایجنتی که این ریپو را
باز می‌کند — Cursor، Codex/GPT، Claude Code، یا هر چیز دیگری — باید این فایل را
بخواند و **دقیقاً** طبق آن عمل کند. محتوای این فایل با `CLAUDE.md` یکی است؛ هر
دو با هم به‌روز می‌شوند (جدول نگهداری در انتهای فایل).

> **دو فایلی که قبل از هر کاری روی محتوا باید خوانده شوند:**
> ۱. همین فایل (نقشه و تریگرها)
> ۲. `.dentcast/workflows/agent-parity.md` (قانونِ رفتاری مشترک — چطور خروجیِ
>    هر ایجنت عیناً مثل خروجی Claude Code در بیاید)
>
> بعد، فایل کانونیِ همان مسیر را **کامل** بخوان. روترها جایگزین آن نیستند.

---

## ۰. قانون طلایی

منطق کار **فقط** در فایل‌های کانونی زیر است. این فایل، `CLAUDE.md`، و
`.cursor/rules/*.mdc` صرفاً **روتر**اند: مسیر را نشان می‌دهند، منطق را تکرار
نمی‌کنند و جایگزین خواندنش نمی‌شوند.

| اگر درخواست کاربر... | فایل کانونی | تریگر نمونه |
|---|---|---|
| **محتوای جدید** برای انتشار دارد (هر نوع: NoteCast، Insight، LiteCast، DentAI، Promptologist، MetaNote، Chairside، PhotoCast، ShareHub، DentCast+، اپیزود، Glossary…) | `.dentcast/workflows/README.md` | «متن جدید دارم»، «این رو منتشر کن» |
| **نسخهٔ انگلیسیِ یک صفحهٔ موجود** را می‌خواهد | `.dentcast/workflows/en-version.md` | «پرامپتولوژیست ۱ رو انگلیسی کن»، «نسخهٔ انگلیسیِ متانوت ۳» |
| **تبلیغ/آگهی** دارد یا می‌خواهد تبلیغی را مدیریت کند | `.dentcast/workflows/ads.md` | «تبلیغ دارم»، «اسپانسر گرفتیم»، «تبلیغ X رو خاموش کن» |
| **آمار تبلیغ** می‌خواهد | `.dentcast/workflows/spot-report.md` | «گزارش تبلیغ بده»، «کلیک اسپانسر X چقدر بوده؟» |
| **ترم دانشنامه** می‌خواهد نوشته شود | `.claude/skills/glossary-writer/SKILL.md` | «ترم Ferrule Effect رو بنویس» |
| **فایل مقاله/پیپر** پیوست کرده (هر نوع محتوا، با یا بدون متن) | `.dentcast/workflows/README.md` — فاز ۰ (روتینگ)، سوال ۴.۷ (مسلح‌شدن)، **گام ۴.۱۰** (سه اکشن) | پیوست‌کردن PDF |

اگر درخواست به هیچ ردیفی نمی‌خورد یا بین چند ردیف مبهم است، **بپرس** — حدس نزن.

> **مسیر فایل‌ها روی گیت‌هاب (اگر لوکال نداری):**
> `https://github.com/dentcast-hub/dentcast-hub.github.io/blob/main/<path>`

---

## ۱. پروتکل انتشار محتوا

وقتی کاربر محتوای جدیدی برای انتشار می‌آورد:

1. `.dentcast/workflows/README.md` را کامل بخوان و **مو به مو** اجرا کن.
2. `dentcast-brain.json` منبع حقیقتِ ساختار، اسکیما و دستهٔ هر نوع محتواست.
   همیشه **آخرین انتریِ هم‌دسته** را الگوی انتری جدید بگیر.
3. **هرگز دسته‌ها را قاطی نکن.** NoteCast در NoteCast می‌ماند، Insight در
   Insight. هر دسته انتری‌اش در بخش خودش و فایلش در دایرکتوری خودش.
4. ویجت «آخرین محتوا» صفحهٔ اصلی، **انتهای** `dentcast-brain.json` را می‌خواند.
   پس هر انتری جدید باید به **انتهای** آرایه اضافه شود — هرگز وسط لیست.
5. هرچه می‌توانی خودت کشف کن؛ فقط چیزی را بپرس که واقعاً قابل استنتاج نیست.
6. **اسکیمای انتری مقدس است.** فیلدی که در انتری‌های قبلیِ همان دسته نبوده،
   اضافه نکن. شکل موجود را عیناً تکرار کن.
7. **رشته‌ای که کاربر داده کپی می‌شود، نه بازتایپ** (Hard Rule 16). نیم‌فاصله
   (`U+200C`) فاصله نیست و فاصله نیم‌فاصله نیست؛ «اندو‌شده» ≠ «اندو شده». اضافه
   یا کم‌کردن یک عبارت به عنوان/کپشنِ کاربر — حتی وقتی دقیق‌تر است — بازنویسی
   است، نه کپی: پیشنهادش بده و تأیید بگیر.
8. **اصلاحِ متن یک «جاروب» است، نه یک ادیتِ نقطه‌ای** (Hard Rule 17). یک عنوان
   دست‌کم روی ۸ سطح زندگی می‌کند (brain، `<title>`، `<h1>`، `og:title`،
   `twitter:title`، `headline`، برچسبِ صفحهٔ فهرست، کارتِ ریلِ صفحهٔ اصلی) و یک
   کپشن روی ۴ (brain، `description`، `og:description` و `twitter:description` —
   روی صفحهٔ fa **و** آینهٔ en). هر اصلاح، همهٔ سطوح را در **همان کامیت** عوض
   می‌کند؛ بعدش بیلدرها و **فاز F** دوباره اجرا می‌شوند.
9. **فاز F — گیت مکانیکی.** publish وقتی تمام است که این سبز باشد:
   ```bash
   python3 tools/verify_publish.py <content_id> \
     --expect-title "<عنوان دقیقاً همان‌طور که کاربر نوشته>" \
     --expect-caption "<کپشن دقیقاً همان‌طور که کاربر نوشته>"
   ```
   خروجیِ غیرصفر یعنی publish ناقص است. هر ردیفِ `FAIL` دستورِ رفعش را خودش چاپ
   می‌کند. خطِ خلاصه‌اش عیناً در گزارش نهایی می‌آید.
10. **هر publish امتیازِ DES می‌گیرد — پرامپت اجرا می‌شود و DOI هرگز حدس زده
    نمی‌شود.** سوال ۴.۸ فاز B مطلب را در یکی از سه سبد می‌گذارد و گام ۴.۱۳ فاز C
    کار را انجام می‌دهد: `.dentcast/dentcast-evidence-score-v1.5.md` به‌عنوان
    system prompt بارگذاری می‌شود (کلِ فایل، منهای appendix — آن appendix قراردادِ
    خطِ لوله است و خودت اجرایش می‌کنی)، **یک بلاک ورودی به ازای هر منبع**، و
    خروجی **عیناً** در `plus/des-scores.json` ذخیره می‌شود. **مدلِ امتیازده هرگز
    fetch نمی‌کند**، پس `source_text` باید **قبل** از اجرای پرامپت resolve شود —
    اول کابینتِ محلی، بعد PubMed MCP، بعد WebFetch — و منبعی که abstractش در
    نیامد `INSUFFICIENT_TEXT` می‌شود، نه امتیازی که از متنِ فارسیِ دنت‌کست دربارهٔ
    آن مقاله ساخته شده باشد. سبدِ سوم همیشه می‌ایستد: مطلبِ پژوهش‌محور **بدون
    مقالهٔ مشخص** (اپیزودی که فقط کپشن و صوت دارد) با دو گزینهٔ نام‌برده پرسیده
    می‌شود — «DOI می‌دم» یا «بدون DES منتشر بشه» — و گزینهٔ سومی وجود ندارد،
    به‌ویژه امتیازدادنش به‌عنوان COMMENTARY برای اینکه چیزی داشته باشیم. اسکیپ‌ها
    فقط LiteCast و همان «بدون DES»ِ کاربرند؛ هر دو گزارش می‌شوند، نه بی‌صدا.

---

## ۲. پروتکل نسخهٔ انگلیسی (تریگر)

تریگر: **«… رو انگلیسی کن»**. در این حالت **از روتر انتشار استفاده نکن**؛
`.dentcast/workflows/en-version.md` را بخوان و طبق آن عمل کن. آن ورکفلو یک صفحهٔ
موجود را در `/{type}/en/{same-filename}.html` آینه می‌کند: chrome انگلیسی از
`metanotes/en/meta-1.html`، بدنه/اسکیمای ارث‌بری‌شده از نوعِ خودِ صفحهٔ منبع، و
تاگل واقعیِ fa↔en روی هر دو طرف. صفحه‌های en ترجمهٔ مستقل و صرفاً برای
خواننده‌اند: **noindex** منتشر می‌شوند (`noindex, follow`، بدون hreflang، خارج
از سایت‌مپ — ترجمهٔ هوش‌مصنوعیِ بازبینی‌نشده‌اند؛ ممیزی سئوی 2026-08-11) و
**نه** انتریِ brain دارند، **نه** خط پالس، **نه** لینک‌دهی تخصصی.

**نکته — روتر انتشار خودش این ورکفلو را صدا می‌زند.** هر publish عادی روی `.org`
با **فاز D** ورکفلوی انتشار تمام می‌شود که همین ورکفلو را روی صفحهٔ تازه‌منتشرشده
اجرا می‌کند؛ پس صفحهٔ جدید **همیشه** با آینهٔ انگلیسی + تاگل fa↔en منتشر می‌شود
(Hard Rule 12). تریگر «… رو انگلیسی کن» برای صفحه‌های **قدیمی‌تر** است که آن
قاعده را ندیده‌اند. **تنها استثنا LiteCast است:** فقط `.ir`، بدون hreflang، بدون
آینهٔ en و بدون تاگل — در هر دو مسیر.

---

## ۳. پروتکل انتشار تبلیغ (تریگر)

تریگر: **«تبلیغ دارم»**، «آگهی دارم»، «اسپانسر گرفتیم»، «تبلیغ X رو خاموش کن»،
«سهم اسپانسر رو کم/زیاد کن». در این حالت **از روتر انتشار استفاده نکن**؛
`.dentcast/workflows/ads.md` را بخوان و طبق آن عمل کن. تبلیغ محتوا نیست: **نه**
صفحه، **نه** انتریِ brain، **نه** خط پالس، **نه** آینهٔ en — کل سیستم در
`spot/spot-config.json` است (مستندات: `spot/README.md`، قیمت‌گذاری:
`spot/PRICING.md`). آن ورکفلو خلأها را می‌پرسد (اسپانسر یا داخلی، لینک، کپی،
عکس، جایگاه‌ها، سهم چرخش) و اسپانسرها را طوری ثبت می‌کند که خودکار با
`rel="sponsored"` رندر شوند (الزام گوگل). **هرگز تبلیغ را داخل صفحه هاردکد نکن.**

**همیشه بپرس این تبلیغ کدام‌یک از چهار «زمان» چرخش را می‌گیرد و بقیه چه پر
می‌شود** — چرخش هر **بازدیدِ صفحهٔ تبلیغ‌دار** یک قدم جلو می‌رود، پس
`rotation.sequence` چهارتایی = چهار بازدید (چهار ریفرش یا چهار مقاله = یک دور
کامل)، و تبلیغی که به هیچ زمانی تخصیص نیافته **هرگز** رندر نمی‌شود
(`ads.md` قاعدهٔ ۶ + سوال ۶ فاز B).

---

## ۴. پروتکل گزارش تبلیغ (تریگر)

تریگر: «گزارش تبلیغ بده»، «آمار تبلیغات»، «تبلیغ‌ها ماه گذشته چقدر دیده شدن؟»،
«کلیک اسپانسر X چقدر بوده؟» → `.dentcast/workflows/spot-report.md`. این مسیر
**فقط خواندنی** است: نه ویرایش config، نه publish. هر رندر/کلیک هم به **API
خودمان** گزارش می‌شود (`spot_impression`/`spot_click` → شمارنده‌های تجمیعی، خواندن
با `GET /admin/spot/stats` — یا آماده‌تر، بخش «گزارش تبلیغ‌ها» در صفحهٔ `GET /admin`
با سه بازهٔ ۲۴ ساعت/۷ روز/۳۰ روز و تفکیکِ هر تبلیغ به محل‌هایش) و هم به GA4
به‌عنوان کنترل متقابل. سه قاعده‌ای که
درستیِ گزارش را تعیین می‌کنند: عدد سرتیتر همیشه **تعداد بارِ نمایش** است (یک نفر
که ۲۰ بار می‌بیند = ۲۰؛ عمداً attribution کاربری وجود ندارد پس «چند نفر»
بی‌پاسخ است)، **پریمیوم صفر است چون طراحی این‌طور است نه چون اندازه‌گیری شده**، و
**هیچ داده‌ای قبل از 2026-07-26 وجود ندارد**. عدد حدس زده نمی‌شود — اگر داده در
دست نیست، دستورش تحویل داده می‌شود.

---

## ۵. پروتکل فایل مقالهٔ پیوست (تریگر — هر نوع، فایل‌محور)

اکشن‌های پیپر با **خودِ فایل** تریگر می‌شوند، نه با نوع محتوا. هر وقت کاربر یک
فایل مقاله/پیپر می‌دهد — با محتوای **هر** نوع (نه فقط DentAI)، یا **بدون** هیچ
متنی — شاخهٔ پیپر فعال می‌شود. جزئیات در **گام ۴.۱۰** فایل
`.dentcast/workflows/README.md` (روتینگ در **فاز ۰**، مسلح‌شدن در **سوال ۴.۷**):

1. **بایگانی در Google Drive** — آپلود در زیرپوشهٔ موضوعیِ درست (انتخاب معنایی)
   از پوشهٔ کابینت:
   `https://drive.google.com/drive/folders/1iDwq4Uj-y7_FO99-QW1Th0hVRN5yfk9f`
2. **غنی‌سازی `dentcast_cabinet_full_catalog.json`** (ریشهٔ ریپو) — افزودن پیپر
   به آرایهٔ `papers` با لینک Drive، هشتگ‌های معنایی (به‌علاوهٔ نام خود مقاله)، و
   اسکیمای enriched.
3. **نویسندهٔ اول ← کردیت DOI** — DOI مقاله را از وب پیدا کن، بعد **نویسندهٔ
   اول** را زیر مطلب با لینک به DOI کردیت بده (سبک ShareHub).

**روتینگ بر اساس چیزی که پیوست شده:**
- **فایل پیپر + متن (هر نوع):** صفحه به‌صورت عادیِ نوعِ خودش منتشر می‌شود **و**
  هر سه اکشن اجرا می‌شوند.
- **فقط فایل پیپر، بدون متن:** **فقط** اکشن ۱ و ۲ (آپلود Drive + به‌روزرسانی
  کاتالوگ) — اکشن ۳ اسکیپ (صفحه‌ای برای کردیت نیست) و بقیهٔ فلوی انتشار هم اسکیپ.
- **متن، بدون فایل پیپر:** هیچ اکشن پیپری؛ انتشار عادی.

گاردِ سختِ این تریگر: **«اگر جایی شک داشتی سوال کن عمل نکن»** — هر جا مطمئن
نیستی (زیرپوشه، موضوع/تگ، DOI، نویسندهٔ اول) **اول بپرس، هرگز حدس نزن.**

---

## ۶. قراردادهای ریپو

- `dentcast-brain.json` — فایل دادهٔ مرکزی؛ یک **آرایهٔ تختِ واحد** از همهٔ
  انتری‌هاست (بدون بخش‌بندی per-type). انتری جدید به انتهای کل آرایه می‌رود؛
  اما **اسکیما** از آخرین انتریِ هم‌دسته گرفته می‌شود.
- `dentcast-hashtag-reference.json` (ریشهٔ ریپو) — **کتابخانهٔ کانونیکالِ
  هشتگ‌ها و تنها منبع حقیقت برای هر `#hashtag`** (جست‌وجوی دستیار هوش مصنوعی از
  آن می‌خواند؛ با `tools/build_plus_index.mjs` در `plus/content-index.json` آینه
  می‌شود). مرجع مالکِ *واژگان* است (کدام فرمِ کانونیکال مجاز است، هرکدام یعنی
  چه، الیاس‌هایش)؛ brain مالکِ *انتساب* (کدام مقاله کدام تگ را دارد).
  **استاندارد، نه متن آزاد:** در هر publish هر هشتگِ پیشنهادی را در برابرِ این
  کتابخانه resolve کن و فرمِ کانونیکالِ موجود را دوباره استفاده کن، یا اگر واقعاً
  نبود یک conceptِ کانونیکالِ نو (با `definition` + `use_when` + `aliases`) بساز
  — الیاس هرگز به‌جای تگ، تگِ کانونیکال هرگز به‌عنوان الیاس، و هیچ concept
  تکراری. ابزار: `tools/hashtag_ref.py` (`--simulate`/`--apply`/`--sync`/`--check`)؛
  batchها در `.dentcast/hashtag-batches/`؛ طبقِ **Hard Rule 15 / گام ۵.۰**ِ
  ورکفلوی انتشار (`.dentcast/workflows/README.md`)، seed‌شده با
  `.dentcast/hashtag-campaign-handoff.md`.
- `dentcast_cabinet_full_catalog.json` — کاتالوگ ریشهٔ کابینت مقالات (آرایهٔ
  `papers`؛ آینهٔ زیرپوشه‌های موضوعیِ Google Drive). با شاخهٔ پیپر (گام ۴.۱۰)
  به‌روز می‌شود؛ رابط جست‌وجو: `dentcast_cabinet_search.html`. تگ‌های
  کباب‌استایلِ `tags`ِ آن یک سیستمِ **جدا** از `#hashtag`های brain بالاست و
  تابعِ کتابخانهٔ مرجعِ هشتگ **نیست**. سه چیزی که کابینت حالا دارد و هیچ
  انتشاری نباید خرابشان کند: **از دسترسِ موتورهای جستجو بیرون است**
  (`robots.txt` هم کاتالوگ و هم همزادِ `.enriched.json` را Disallow می‌کند،
  `_headers` هدرِ `X-Robots-Tag: noindex` می‌دهد، و خودِ صفحه
  `<meta robots noindex>` دارد) — این را «درست» نکن و هرگز واردِ sitemap نکن؛
  روی سایت **پریمیوم** است («کتابخانهٔ دنت‌کست»، کارت دومِ بخش «موتور جستجوی
  دندانپزشکی»ِ تب آرشیو و ابزارِ «کتابخانه» در همبرگری) و گیتِ تپ در
  `plus/js/library-gate.js` است (از `plus/js/header.js` روی هر صفحه نصب
  می‌شود)، پس پیپر جدید را با باز کردنِ مستقیمِ صفحهٔ جستجو تأیید کن؛
  **گیت به مقصد بسته است، نه به یک المان**: یک شنوندهٔ delegated هر لینکی را
  که مسیرش کابینت است می‌گیرد، به‌علاوهٔ کارتِ آرشیو که `div[role=button]` است
  و تنها دری است که نشانِ قفل دارد. تا ۲۰۲۶-۰۸-۰۹ اسکریپتی inline فقط روی
  `#card-library` بود و همین بود که آیکنِ توپ‌بارِ دسکتاپ و «کتابخانه»ی
  همبرگری را باز می‌گذاشت — هرگز دوباره به یک id گره‌اش نزن؛ و کارت و شیتِ گیتش یک **شمارِ دستیِ مقالات**
  را تبلیغ می‌کنند («بیش از ۲۲۰۰ مقاله») که گام ۴.۱۰ با عبور از هر صدِ رُند،
  روی هر دو سطح با هم به‌روزش می‌کند.
- `tools/` — اسکریپت‌های پایتون/نود، از جمله بیلدر اصلی ایندکس.
- **فهرست موضوعی یک URL است با دو چیدمان** (`/pillar/` و `/pillar/<slug>/`).
  `tools/build_pillar.py all` برای هر پیلارِ ساختارمند **دو** فایل می‌نویسد: خودِ
  **صفحه**، که همهٔ مطالبِ آن پیلار را تخت و از تازه‌ترین فهرست می‌کند (بی‌تاریخ‌ها
  ته لیست) و همان چیزی است که هر بازدیدکننده و هر خزنده می‌گیرد؛ و
  **`structure.json`** کنارش، که زیرموضوع‌بندی و ترتیبِ خواندن را نگه می‌دارد.
  `/pillar/premium-index.js` این sidecar را می‌گیرد و روی **همان** URL جایگزین
  می‌کند، فقط بعد از اینکه `/me` اشتراک پریمیوم را تأیید کند — نه صفحهٔ دوم، نه
  ریدایرکت، نه noindex. **هرگز کارت‌های زیرموضوعی را دستی به صفحهٔ پیلار برنگردان
  و هرگز `structure.json` را دستی ویرایش نکن**: هر دو چیدمان در یک سند یعنی همان
  لینک‌ها دو بار روی صفحه، که دقیقاً همان مشکلِ محتوای تکراری است که این تفکیک
  برای پرهیز از آن ساخته شده. سه پیامدِ ماندنی: تاریخ از `datePublished` داخل
  JSON-LD خودِ هر صفحهٔ مقصد می‌آید (هیچ جای دیگری ندارد) و **در زمان build** به
  تقویم شمسی تبدیل می‌شود، چون خزنده باید تاریخ را بخواند نه محاسبه کند؛
  `tools/build_plus_index.mjs` تاکسونومیِ زیرموضوع را از همین sidecarها می‌خواند،
  پس باید **بعد از** بیلدر پیلار اجرا شود؛ و `robots.txt` این sidecarها را
  Disallow می‌کند، که برای لایهٔ پریمیوم هیچ هزینه‌ای ندارد چون قواعد robots هرگز
  شاملِ fetch خودِ صفحه نمی‌شوند.
- **مُهرِ نسخهٔ کش.** فایل‌های مشترک با آدرسِ مُهرخورده لود می‌شوند
  (`/dc-nav.js?v=40`، `/plus/js/pricing-page.js?v=2`)؛ مسیر هیچ‌وقت عوض نمی‌شود،
  پس تنها چیزی که مرورگرِ بازدیدکنندهٔ برگشته را وادار به گرفتنِ فایلِ تازه
  می‌کند همین مُهر است. بعد از تغییرِ **هر** فایلِ مشترک — از جمله ماژولی که
  یک ورودیِ مُهرخورده فقط *import*ش می‌کند، مثل `plus/js/premium-cta.js` — در
  همان کامیت `python3 tools/asset_version.py --bump` را اجرا کن. کلِ گرافِ
  import هر فایل را هش می‌کند، نسخه‌های کهنه را بالا می‌برد، همهٔ صفحه‌ها را
  بازنویسی می‌کند و `.dentcast/asset-versions.json` را به‌روز می‌کند. CI با
  `--check` بررسی می‌کند. **چهار فایل را هیچ صفحه‌ای مُهر نمی‌زند**:
  `plus/plus.js` و سه فایلِ `plus*.css` را `dc-nav.js` در زمانِ اجرا اضافه
  می‌کند، پشتِ یک ثابتِ دستی (`var V`). حالا ابزار مالکِ آن ثابت هم هست
  (`dc-nav.js:V` در manifest، یک fingerprint روی هر چهار گراف، که **قبل** از
  پاسِ HTML بالا می‌رود تا `/dc-nav.js?v=` در همان اجرا جابه‌جا شود) — پیش از
  این، تغییرِ `plus.js` فایلی منتشر می‌کرد که هر مرورگرِ برگشتی و CDN از کش با
  مُهرِ قدیمی سرو می‌کردند: دیپلوی‌شده، زنده، و نامرئی؛ و فقط وقتی پیدا شد که
  کسی گفت یک قابلیت نیست (۲۰۲۶-۰۸-۱۲).
- `index.html` — صفحهٔ اصلی با بخش Pulse و ویجت آخرین محتوا.
- هر نوع محتوا دایرکتوری خودش را در ریشه دارد (`/notecast/`، `/insight/`،
  `/litecast/`، …). مسیر دقیق را از URLهای داخل انتری‌های brain تأیید کن.
- `.dentcast/workflows/` — ورکفلوهای انتشار (+ `agent-parity.md`).
- **up-board — نمای مرتب‌شده به رأی خواننده‌ها** (`/up-board/`). **سه سطحِ
  دسترسی، و مرزِ بینشان خودِ طراحی است:** صفحه و «تازه‌ترین» **عمومی**‌اند (هر
  ۴۴۴ لینک، از تازه‌ترین)؛ زدنِ قلب و دیدنِ شمارِ قلبِ یک صفحه فقط حسابِ
  **رایگانِ واردشده** می‌خواهد — پول گرفتن برای خودِ کنش، تخته را از سیگنالی که
  با آن رتبه می‌دهد گرسنه می‌کرد؛ و «بالاترین»، یعنی چیدمانِ رتبه‌بندی‌شده
  (`GET /votes/board` با `requireAuth` + `requirePremium`)، **پریمیوم** است.
  گیت روی **چیدمان** است نه محتوا — همان تقسیمی که `pillar` از قبل دارد، پس یک
  قاعده است که دو بار اعمال شده نه یک قاعدهٔ تازه. تبش **امبر** می‌پوشد (هرگز
  آبی: امبر در کل سایت یعنی «این چیزی است که اشتراک می‌خرد») و هرگز حذف یا
  غیرفعال نمی‌شود — خواننده‌ای که نمی‌تواند بازش کند هم باید بداند چیدمانِ دوم
  وجود دارد، وگرنه چیزی برای خواستن نیست. گیت **سه** جواب را از هم جدا می‌کند و
  یکی‌گرفتنِ هر دوتاشان باگ واقعی است: ۴۰۱ یعنی خارج‌شده (ورود اول می‌آید و
  لینکِ خرید آرام‌تر بعدش — ممکن است همین حالا مشترک باشد و فقط روی این دستگاه
  خارج شده)، ۴۰۲ یعنی رایگان (کارتِ پریمیوم)، و هر چیز دیگری یعنی «نتوانستیم
  بپرسیم» که **هرگز** نباید به‌شکل تبلیغِ خرید دربیاید (یک ری‌دیپلوی چند دقیقه
  دقیقاً همین است؛ همان دلیلی که `unreachableGate` در `premium-cta.js` برایش
  هست). کارتِ گیت **اول می‌گوید چیدمان چیست، بعد می‌گوید چقدر است** — هیچ‌کس
  ترتیبی را که ندیده نمی‌خرد — و لینکِ `?sort=top` به فهرستِ رایگان برمی‌گردد
  بدون اینکه شیت را سرخود باز کند. `GET /votes/board` هیچ `cache-control`
  خودش نمی‌گذارد: `server.ts` روی هر پاسخ `no-store` می‌زند و این invariantِ
  سراسری را جوابی که به tier وابسته است بیشتر از بقیه لازم دارد. یک صفحه با **دو چیدمان** از همان ۴۴۳ لینک: «تازه‌ترین» و
  «بالاترین». عمداً یک سند است نه دو URL — همان تصمیمی که `pillar` گرفت و به
  همان دلیل. سه قطعه، و تقسیمِ بینشان خودِ طراحی است: **`up-board/index.json`**
  کاتالوگ است (id/عنوان/نوع/URL/تاریخ) و تنها نویسنده‌اش
  `tools/build_upboard_index.py` است که در هر انتشار اجرا می‌شود (گام ۵c
  ورکفلو) — هرگز دستی ویرایشش نکن؛ **`GET /votes/board`** ترتیب است، فقط id و
  عدد، پس تغییر یک عنوان به دیپلوی API نیاز ندارد؛ **`content_votes`**
  (migration 0040) تنها چیزی است که نوشته می‌شود، چون قلب پس‌گرفتنی است و
  `user_activity` فقط append است. بقیه همه **مشتق** است، از جمله **تعاملی** که پشت هر
  رتبه‌بندی است — کاربرانِ متمایز به ازای هر کنش
  (گفت‌وگو/هایلایت/پین/اشتراک/تکمیل)، محاسبه‌شده از `user_activity` **و
  `support_tickets`**، هرگز ذخیره‌نشده. **گفت‌وگوی زیر مطلب با وزن ۴ صدرِ
  نردبان است**، بالاتر از هایلایت، بر همان قاعده‌ای که بقیه را به‌ترتیبِ هزینه
  برای خواننده می‌چیند: هایلایت یک ثانیه است و کسی نمی‌بیندش، پرسش چند دقیقه
  نوشتن است که به یک آدم می‌رسد و پای نامِ نمایشیِ خودِ خواننده. تا
  ۲۰۲۶-۰۸-۱۲ برای امتیاز **نامرئی** بود — نه کم‌وزن، بلکه غایب — چون نوشتنِ
  کامنت هیچ ردیفی در `user_activity` نمی‌نویسد و XP نمی‌دهد. دو خاصیت از
  اسکیما می‌آید نه از فیلتر: **پاسخِ مؤسس هرگز شمرده نمی‌شود** (`user_id` هر
  تیکت خودِ خواننده است و پاسخ یک ردیفِ `ticket_messages` است که تیکتی
  نمی‌سازد، پس پاسخگو بودن نمی‌تواند صفحه‌های خودت را بالا ببرد) و **هر
  خواننده یک صداست** هرچقدر هم گفت‌وگو طول بکشد. **نوشتن** شمرده می‌شود نه
  عمومی‌شدن — `is_public` تصمیمِ ویراستاریِ مؤسس است و وزن‌دادن به آن، قضاوتِ
  او را داخل رتبه‌بندی‌ای می‌برد که کلِ ادعایش این است که از خواننده‌ها می‌آید. به‌صورت **صدک** وارد امتیاز
  می‌شود (۱ تا ۱۰۰، در پاسخ با کلید `engagement` و روی هر ردیف با یک شیتِ
  توضیح که با لمس باز می‌شود)، نه عددِ خام: اعداد خام آن‌قدر چوله‌اند که هر
  مقیاسِ متکی به بیشینه، وسطِ سایت را به عددِ تک‌رقمی له می‌کند — و صدک تنها
  شکلِ ۰ تا ۱۰۰ از این است که می‌شود بلند گفتش بدون دروغ، چون **هیچ‌جای سایت
  شمارِ بازدیدِ per-article نداریم** (`view_stats` به‌ازای روز و کلاسِ
  بازدیدکننده است و `spot_stats` به‌ازای جایگاهِ تبلیغ)، پس «٪ خواننده‌ها»
  درصدی از هیچ می‌شد. **و این صدک درونِ یک کلاس گرفته می‌شود**
  (`contentClass()`: `episodes/` صوتی، بقیه خواندنی)، چون صفحهٔ صوتی بیشترِ
  seed را نمی‌تواند بگیرد — `highlight` و `pin` هر کدام سه امتیازند و هیچ‌کدام
  روی پادکست دری ندارند، پس روی یک مقیاسِ مشترک شاخصش این را می‌سنجید که چند
  راه برای نشان‌دادنِ توجه داشته، نه چقدر توجه گرفته. همان استدلالی که اصلاً
  تعامل را صدک کرد، یک لایه عمیق‌تر: عددهایی که قابل‌مقایسه نیستند با رتبه
  مقایسه می‌شوند. عمداً **وزن نیست** — «شنیدن ده می‌ارزد» جوابِ بدیهیِ دیگر بود
  و همان اشتباهِ سقفِ ثابت: ده روی ترکیبِ امسال کالیبره می‌شود، روزی که
  پادکست‌ها شلوغ شوند غالبشان می‌کند و روزی که ساکت شوند کاری نمی‌کند، و هر
  ۲۱۰ تا را با هم بالا می‌برد به‌جای اینکه از هم جدایشان کند. `MIN_CLASS_SIZE`
  (۱۲) تنها نگهبان است — صدک روی سه صفحه فقط ۳۳ و ۶۷ و ۱۰۰ می‌گوید — و کلاسِ
  کوچک‌تر از آن در مقیاسِ کلِ سایت رتبه می‌گیرد؛ `Board.engagement_scope`
  می‌گوید کدام کلاس‌ها درونی رتبه خورده‌اند، چون جملهٔ شیت («بیشتر از ۶۲٪
  **پادکست‌ها**» در برابر «از مطالبِ سایت») در دو حالت دو ادعای متفاوت است و
  صوتی روز اول دوازده صفحهٔ تعامل‌دار ندارد. آن صدک حداکثر `engagement_cap` امتیاز می‌خرد، و سقف
  **نسبی** است: `max(CAP_FLOOR, CAP_SHARE × میانگینِ قلبِ هر صفحهٔ رأی‌خورده)`.
  سقفِ ثابت فقط یک‌بار می‌توانست درست باشد — سه امتیاز وقتی پرقلب‌ترین صفحه پنج
  قلب دارد بیشترِ رتبه‌بندی است و وقتی چهارصد قلب دارد نویز — پس نسبت‌دادنش به
  میانگین، **اثرش** را ثابت نگه می‌دارد نه مقدارش را. `CAP_FLOOR` همان چیزی است
  که روزِ اول را ممکن می‌کند: بدون رأی، جمله‌ی نسبی صفر است و کفْ تنها چیزی است
  که تخته را مرتب می‌کند تا رأی‌ها برسند. **سقف همان تضمین است، و دلیلِ اینکه
  سقف است نه ضریب: صفحه‌ای که قلبش از سقف بیشتر باشد اصلاً با تعامل قابلِ
  سبقت نیست** — قلب برای همیشه سیگنالِ ارشد می‌ماند، بدون محوشدنی که زمان‌بندی
  بخواهد و بدون روزِ سوییچ. ترتیب `امتیاز ← قلب ← تعامل ← id` است؛ قلب پیش از
  تعامل می‌آید تا سیگنالِ ارشد حتی داخلِ تساوی هم ارشد بماند. چهار قاعده که کد به آن‌ها متکی است: **صفر هرگز چاپ نمی‌شود** — قلبِ
  خالی دعوت می‌کند، «۰ ❤» اعلام می‌کند که هیچ‌کس اهمیت نداده، و آستانه ۱ است نه
  ۵ تا عددِ چاپ‌شده هرگز بین دو نفر که به یک صفحه نگاه می‌کنند فرق نکند؛ **رأی
  هیچ امتیازی نمی‌دهد** — نه XP، نه استریک، نه ردیفی در `user_activity` (همان
  استدلالی که `score.ts` برای کنار گذاشتن `content_shared` می‌آورد، برای یک
  فشارِ یک‌ثانیه‌ای دوچندان صدق می‌کند، و امتیاز هرگز کم نمی‌شود)؛ **رأی منفی
  وجود ندارد** و ستونی هم برایش باز نگذاشته‌ایم — فلش پایین ابزار مدیریت است
  برای سایتی که مطلبِ دیگران را منتشر می‌کند، و اینجا فقط کیس‌های بالینیِ خودِ
  مؤسس را در ملأعام بد علامت می‌زند؛ و **صفحه `noindex, follow` است، بیرون از
  سایت‌مپ، با کاتالوگی که در `robots.txt` مسدود است** — هر لینکش جای دیگری
  ایندکس شده و آنچه اضافه می‌کند ترتیبی است که خزنده نمی‌تواند بازتولیدش کند.
  سطح‌ها: دکمهٔ قلب در ردیفِ کنشِ خودِ مقاله (`plus/js/votes.js` آن را داخل
  `#dcActionRow .dc-actions-main` سوار می‌کند؛ در پوستهٔ دسکتاپ که dc-nav.js حذف
  شده، خودِ `plus.js` آن ردیف را می‌سازد — همان شکافی که `buildShareButton` پر
  می‌کند)، خودِ صفحه (`plus/js/upboard-page.js`)،
  و تب «بالاترین» + لینک «همه ›» در باکس `.dc-monitor` صفحهٔ اصلی
  (`plus/js/home-upboard.js`). گرید **«دسته‌های محتوا» در صفحهٔ اصلی جابه‌جا یا
  حذف نمی‌شود**: `renderHome` در `spot.js` تبلیغ موبایل را به `.dc-exa-cats`
  لنگر می‌اندازد، پس دست زدن به آن موجودیِ اسپانسرِ پولی را جابه‌جا می‌کند. رنگ
  `--heart` است، یک توکن معناییِ سوم — هرگز کهربایی (که یعنی «این چیزی است که
  اشتراک می‌خرد» و up-board رایگان است) و هرگز آبیِ برند (قلبِ آبی، قلب خوانده
  نمی‌شود). تست‌ها: `plus-api/test/votes.test.ts` + `votes.dom.test.ts` +
  `upboard-page.dom.test.ts` + `home-upboard.dom.test.ts`.
- `spot/` — سیستم تبلیغاتِ config-محور («Spot»؛ نام عمداً خنثی است — قاعدهٔ
  عمومیِ `##.dc-ad` در EasyList نام‌گذاری قدیمیِ `/ads/` + `dc-ad` را زیر هر
  adblocker پنهان می‌کرد، پس **هرگز** از «ad/ads» در نام فایل، مسیر، کلاس، id یا
  data attribute این سیستم استفاده نکن). `spot/spot-config.json` تنها منبع حقیقت
  است (کلید اصلی + هر جایگاه، creativeهای premium/sponsor، `rotation.sequence`،
  targeting با `slots`/`audience`)؛ `spot/spot.js` را یک loader hook در انتهای
  `dc-nav.js` روی همهٔ صفحه‌ها تزریق می‌کند. کاربر ناشناس و Plus رایگان تبلیغ
  می‌بینند؛ پریمیوم نه؛ هر چیزِ خاموش هیچ ردی نمی‌گذارد. **هیچ صفحه‌ای مارک‌آپ
  تبلیغ ندارد.**

---

## ۷. سطوح Plus و Premium (چه چیزی با هر publish باید به‌روز شود)

این بخش دقیقاً همان چیزی است که یک publishِ «عادی» را به یک publishِ کاملِ
Plus/Premium تبدیل می‌کند. همهٔ این‌ها گام‌های خودِ ورکفلوی انتشارند — اینجا فقط
نقشه‌شان است:

| سطح | چه چیزی | کجا | چه زمانی به‌روز می‌شود |
|---|---|---|---|
| عادی | صفحهٔ محتوا + brain + پالس + پیلار | `/{type}/`، `dentcast-brain.json`، `index.html` | هر publish |
| عادی | آینهٔ انگلیسی + تاگل | `/{type}/en/` | فاز D — هر publish روی `.org` (جز LiteCast) |
| Plus | درخت ناوبریِ «میز کار» | `plus/content-index.json` | `node tools/build_plus_index.mjs` — هر publish که brain را تغییر داده |
| Plus/Premium | فلش‌کارت (Leitner) | `DefinedTermSet` در `plus/faq-corpus.json` (نه روی خودِ صفحه) → `plus/flashcards-index.json` | گام ۴.۱۱ + `node tools/build_flashcards_index.mjs` |
| Plus/Premium | کوییز نمره‌دار | `FAQPage` در `plus/faq-corpus.json` (نه روی خودِ صفحه) → `plus/quiz-index.json` | گام ۴.۱۲ + `node tools/build_quiz_index.mjs` |
| عادی | امتیاز شواهد (DES) | `plus/des-scores.json` (نه brain — Hard Rule 5؛ نه مارک‌آپِ صفحه) | سوال ۴.۸ + گام ۴.۱۳ — هر publish (جز LiteCast و «بدون DES»ِ کاربر) |
| Premium | مسیرهای یادگیری (pathways) | `plus/pathways.json` | گام ۵.۶ — هر publish تخصصی (جز LiteCast) |
| Premium | کالکشن‌ها | دیتابیس API (`collections`/`collection_items`) | کاربر می‌سازد؛ publish کاری ندارد |
| Premium | دفترچهٔ هایلایت‌ها | دیتابیس API (`highlights`) → `/plus/highlights.html` | کاربر می‌سازد؛ publish کاری ندارد |
| Premium | کتابخانهٔ کابینت | `dentcast_cabinet_full_catalog.json` → `dentcast_cabinet_search.html` | فقط **گام ۴.۱۰** (پیپر پیوست‌شده) — publishِ بی‌پیپر کاری با آن ندارد |
| Plus/Premium | نوتیف مطلب جدید | `<meta name="dc-notify" content="true">` | فاز E — جز paper-only/LiteCast/glossary |

جزئیات معماری:

- `plus/pathways.json` — مسیرهای یادگیری DentCast Plus (اسکیمای §۵ اسپک؛ فاز ۳
  پریمیوم). یک pathway یک **سفر یادگیری منتخب** است، نه نمای pillar: برخلاف
  pillar (هر آیتم یک خانه)، **یک آیتم می‌تواند در چند pathway باشد**، هر بار در
  جای درستِ پیش‌نیاز→پیشرفته. هر publish تخصصی در **گام ۵.۶** محتوای جدید را به
  pathwayهای مربوطه تخصیص می‌دهد (تصمیم **معنایی**؛ اگر شک داشتی بپرس). ابزارها:
  `tools/pathway_place.py` (پیشنهاد جایگذاری + `--insert` + `--coverage`) و
  `tools/pathway_scout.py` (جست‌وجوی کاندید + `--steps` + `--coverage`).
  کاتالوگ/داکترین: `reports/pathways-catalog-2026-07-22.md`. بک‌اند فعال است —
  `plus-api/src/pathways.ts` آن را می‌خواند (کش‌شده، reload-on-change مثل
  `content-index.ts`) و `GET/POST /pathways*` (پریمیوم‌گیت) و `active_pathway` در
  `GET /me` از همان می‌خوانند؛ `user_pathways` ثبت‌نام + کشِ مشتقِ
  `current_step`/`completed_at` را نگه می‌دارد (هرگز با فراخوانی کلاینت جلو
  نمی‌رود — در هر خواندن از highlightها/`user_activity` بازمحاسبه می‌شود).
  ویرایش این فایل دیگر redeploy API نمی‌خواهد: نسخهٔ بیک‌شده در `PATHWAYS_PATH`
  فقط مقدار بوت است و `plus-api/src/content-refresh.ts` هر چند دقیقه فایل
  منتشرشدهٔ `plus/pathways.json` (و `plus/content-index.json`) را از خود سایت
  دوباره می‌گیرد — با اعتبارسنجی، فقط رو به جلو، و در هر شکستی آخرین نسخهٔ سالم
  می‌ماند. فرانت هم فعال است:
  `/plus/pathways.html` (کاتالوگ)، `/plus/pathway.html?id=` (فهرست گام‌ها +
  ثبت‌نام)، و بلاک «مسیر یادگیری» در داشبورد `/plus/`
  (`plus/js/pathways.js` رندرکنندهٔ مشترک). **باندل‌های شروع** (ردیف‌های
  `kind: "bundle"`، بعد از ۱۵ مسیرِ کامل append می‌شوند) زیرمجموعه‌ای کوتاه و
  منتخب از گام‌های یک pathway‌اند — «فقط هستهٔ موضوع» در ۵ تا ۸ قدم، نه یک
  pathway جدید. اسکیما فقط روی شیءِ pathway فیلدهای `kind`/`glyph`/
  `prereq_bundle`/`continues_pathway` اضافه می‌کند (steps همان
  `{content_id, milestone}` می‌ماند)؛ کاتالوگ، داکترین، و قاعدهٔ سرمقاله‌ایِ
  «چه چیزی وارد باندل می‌شود» در `.dentcast/bundles-handoff.md` است.
  **گام ۵.۶ محتوای جدید را به pathwayهای کامل تخصیص می‌دهد؛ گام 5.6-ب بعدش
  زنجیرهٔ گیتِ باندل را اجرا می‌کند** — پنج گیتِ مکانیکیِ بله/خیر (نوع پوشه
  فقط episodes/notecast/sharehub/dentai؛ عنوانْ یک مفهوم کامل نه کیس/ترفند/
  نکته؛ پرکردن حفره‌ای که هیچ قدم فعلی نمی‌پوشاند؛ نبودن دوقلو در باندل؛
  حداکثر ۸ قدم بعد از ورود). هر پنج پاس → ورود خودکار وسط لیست با انکر
  معنایی (هرگز انتهای لیست — قدم آخر صاحب 🏁 milestone است)؛ هر ابهام، هر
  جایگزینی، یا جایگاه انتهای لیست → سؤال از بنیان‌گذار. گزارش publish برای
  هر باندلِ کاندید حکم صریح می‌نویسد، و هر ویرایش باندل در همان کامیتْ
  شمارهٔ قدم `plus/js/home-bundles.js` را هم به‌روز می‌کند +
  `asset_version.py --bump`. روی صفحهٔ اصلی به‌شکل ریلِ «از کجا شروع کنم؟»
  (`plus/js/home-bundles.js`) پیش از بخش «باندل‌های شروع» در کاتالوگ دیده
  می‌شود.
- `plus/badges.json` — کاتالوگ نشان‌های بخش «افتخارات» پروفایل (۲ مدال لیگ + ۲۵ نشان). همان ماشینِ `pathways.json`: در ریپو نسخه‌بندی می‌شود، `plus-api/src/badges.ts` می‌خواندش (reload-on-change، نسخهٔ منتشرشده برنده، در هر شکستی آخرین نسخهٔ سالم)، و `content-refresh.ts` زنده دوباره می‌گیردش — پس **تغییر یک آستانه یا بازنویسی متنِ فارسیِ یک نشان، یک کامیت به سایت است نه deploy API**. سرو با `GET /achievements` (`plus-api/src/routes/achievements.ts`)، رندر با `plus/js/achievements.js` داخل `profile.js` بین «رکوردها» و «لیگ من». چهار قاعده‌ای که کد رویشان بنا شده: **هر نشان مشتق می‌شود، هرگز ذخیره نمی‌شود** (`plus-api/src/services/achievements.ts` هر معیار را از دادهٔ موجود حساب می‌کند) — همین است که کل قفسه را گذشته‌نگر می‌کند، بدون ستون `earned_at` که بتواند با لاگِ مبدأش اختلاف پیدا کند و بدون backfill در روز انتشار؛ **یک مقایسه و فقط یکی، `value >= threshold`**، پس هر معیارِ ذاتاً برعکس (رتبهٔ ثبت‌نام در «پیشگام») در سرویس به ۰/۱ تبدیل می‌شود نه اینکه ارزیابی مسیر دوم پیدا کند؛ **سطح یک رنگِ حلقه است، نه یک کلمه** — دیوار هرگز «طلا» نمی‌نویسد، فقط ردیفِ مدال می‌نویسد و آن‌جا هم هرگز تنها نمی‌آید («طلای کامپوزیت»)، چون دو مقیاسِ فلزی در این بخش کنار هم‌اند (هفت مادهٔ دندانیِ لیگ و برنز/نقره/طلای نشان‌ها) و کلمه همان چیزی‌ست که تصادف می‌کند؛ و **هر کوئریِ متکی به رتبهٔ لیگ فیلترِ اعتبار را دارد** (اندازهٔ گروه `>= min_valid_group_size` **و** `weekly_xp > 0`) چون `league-finalize.ts` برای همهٔ اعضای همهٔ گروه‌ها `final_rank` می‌نویسد، حتی گروه‌هایی که خود UI غیررقابتی اعلامشان می‌کند — بدون این فیلتر، اولین مدالِ بیشترِ حساب‌ها بابتِ هیچ صادر می‌شود. `visibility` تعیین می‌کند نشانِ کسب‌نشده چه شکلی‌ست: `always` (خاکستری، با شرطش)، `mystery` (کاشیِ «؟» که هیچ نشتی ندارد — سرور نام و آیکون و متن را حذف می‌کند)، `earned_only` (اصلاً نیست، برای شرطی که دیگر برای کسی قابل‌کسب نیست). **کلاسِ اهدایی** (مهاجرت 0037): نشانی که متریکش `grant:<کلیدِ خودش>` است — «همراه» (کلید `companion`) اولین عضو — اهدای بنیان‌گذار است، نه کسب‌کردنی: چیزی که نوشته می‌شود تصمیم است (یک ردیفِ `badge_grants` از `POST /admin/badges/grant`، با فرم روی صفحهٔ `GET /admin`؛ پس‌گرفتن با `POST /admin/badges/revoke`، خواندن با `GET /admin/badges/grants?user=`) و خودِ نشان همچنان از آن مشتق می‌شود (`plus-api/src/services/badge-grants.ts` + جمعِ عمومیِ `grant:*` در `achievements.ts`) — همان حرکتی که «ستون» با دفترِ payments می‌کند. اهدا برای هر (کاربر، نشان) یک‌بار است و فشردنِ دوباره هیچ‌چیزی نمی‌سازد؛ تخفیفِ یک‌بارمصرفِ اختیاری به‌صورت یک ردیفِ عادیِ `discount_grants` در همان تراکنش همراهش می‌آید (عرفِ تشکرِ گزارشِ باگ ٪۵ است)، چون نشانِ اهدایی همیشه `earned_only` و تک‌سطحی است و هرگز `discount_percent` روی خودش ندارد. تست‌ها: `plus-api/test/badge-grants.test.ts`. **«ستون» (کلید `pillar`) تنها نشانی‌ست که امتیازی بیرون از دیوار به آن گره خورده**: خریدارانِ اول، برای همیشه و در هر قیمتی، تمدید را ۲۰٪ ارزان‌تر می‌خرند. `PILLAR_SEATS` عدد ۵۱ است — پنجاه صندلی برای خواننده‌ها به‌علاوهٔ حسابِ خودِ بنیان‌گذار که از همان درگاه می‌خرد — و متنِ عمومی همه‌جا «پنجاه» می‌ماند، که برای خواننده‌ها همچنان درست است. تعریفِ صندلی در `plus-api/src/services/pillar.ts` است و همه‌چیز از همان می‌خواند — متریکِ نشان، مبلغِ `startPayment` و قیمت‌های شخصی‌شدهٔ `GET /pay/plans` — هرگز جای دیگری دوباره مشتقش نکن. خریدِ اول هرگز تخفیف نمی‌گیرد (صندلی با همان خرید ساخته می‌شود) و گیفت‌کارت/هدیهٔ ادمین/جایزهٔ لیگ صندلی نمی‌سازند (فقط ردیف‌های `payments`) — **با یک استثنا: اهدای خودِ بنیان‌گذار** (`pillar_grants`، migration 0038؛ `POST /admin/pillar/grant` و برگرداندنش با `POST /admin/pillar/revoke`)، برای کسی که در ساختنِ این‌جا کمک کرده و هرگز در دفترِ پرداخت‌ها ظاهر نمی‌شود. همان حرکتِ «کلاسِ اهدایی»ِ بالاست، یک لایه پایین‌تر — چیزی که نوشته می‌شود **تصمیم** است و صندلی همچنان از آن مشتق می‌شود — ولی جدولِ خودش را دارد نه یک ردیفِ `badge_grants`، چون صندلی فقط یک نشان نیست: هم رتبه‌ای میان پنجاه نفر است و هم قیمتی سرِ صندوق، و هر دو باید از یک کوئری بیایند. عمداً ردیفی **کنارِ** دفتر است نه یک `payments` جعلی — که به درآمد، به سقفِ ماهانهٔ درگاه و به joinِ اعتبارهای تخفیف دروغ می‌گفت؛ هر دو منبع در **یک** کوئری رتبه می‌گیرند (`SEAT_CLAIMS` در `pillar.ts`)؛ اهدا امروز نوشته می‌شود پس بعد از همهٔ صندلی‌های گرفته‌شده می‌نشیند و **جای هیچ‌کس را عوض نمی‌کند**؛ یکی از ۵۱ صندلی را **خرج** می‌کند نه اینکه ظرفیت را بزرگ کند (در هر دو حالت پنجاه خواننده صندلی دارند، و همین است که «پنجاه»ِ متنِ عمومی را راست نگه می‌دارد)؛ حسابی که هر دو ادعا را داشته باشد ادعای زودترش را نگه می‌دارد، پس خریدِ بعدی صندلی دوم نمی‌سازد؛ و تنها صندلیِ قابلِ پس‌گرفتن است، چون هدیه رسید نیست. دو پیامد که باید صریح گفته شوند: خریدِ **اولِ** دارندهٔ صندلیِ اهدایی هم تخفیف می‌خورد (صندلی قبل از خرید وجود دارد — همین هدیه است)، و همان پیامِ «تو ستون شدی» را می‌گیرد که خودِ اندپوینتِ اهدا می‌فرستد مگر با `welcome: false` صدا زده شود. **وضعیتِ پرشدنِ صندلی‌ها یک راز است**: نشان از روزِ اول `earned_only` است و هیچ سطحِ کاربری هرگز نمی‌گوید صندلی مانده یا نه، چون هر دو جواب یک آمارِ مشترک است («باز» یعنی زیرِ ۵۰ حسابِ پولی، «بسته» روزِ پنجاهمی را تاریخ می‌زند) — بسته‌شدن خودکار است (`isPillarSeat` در ۵۰ می‌ایستد، بدون کامیت و بدون اعلان)، کمپین در بازاریابیِ خودِ بنیان‌گذار زندگی می‌کند، و تنها سطحی که فهرست را گزارش می‌کند `GET /admin/pillar` است؛ هرگز علامتِ باز/بسته به دیوار یا صفحهٔ قیمت اضافه نکن. **دیوار پول هم دارد — اعتبارهای تخفیفِ یک‌بارمصرف** (`plus-api/src/services/discount-credits.ts`، migration 0031): نشانِ سطح‌داری که به **نقره برسد ٪۱ و به طلا ٪۲** تخفیفِ یک خریدِ آینده می‌سازد (`discount_percent` روی همان سطح در `badges.json` — تغییرش مثل هر آستانه‌ای یک کامیت است)، و برنزها، تک‌سطحی‌ها و mysteryها عمداً هیچ نمی‌سازند (پول‌دادن به «ققنوس» یعنی پول‌دادن به شکستنِ استریک، و برچسبِ ارزش روی کاشیِ «؟» رازش را می‌فروشد). موتور عمداً **عمومی** است: ردیف‌های `discount_grants` همان اعتبارها هستند با تصمیمِ بنیان‌گذار — تولد، عیدی — از طریق `POST /admin/discounts/grant` (خواندن با `GET /admin/discounts?user=`)، پس «بعداً برای چیزهای دیگر هم تخفیف بدهیم» یک insert است، نه یک فیچر. چهار قاعده از پول محافظت می‌کنند: **مجموعِ اعتبارهای اعمال‌شده در یک خرید هرگز از `CREDIT_CAP_PERCENT` (ده) نمی‌گذرد** و اعتباری که جا نشود کامل کنار می‌رود و برای خرید بعد می‌ماند — هرگز بریده نمی‌شود، و دقیقاً به همین دلیل هدیه‌ای که از سقف بزرگ‌تر باشد در لحظهٔ اهدا به چند اعتبار شکسته می‌شود (`splitGrantPercent`، مشترکِ هر دو نویسنده: ٪۲۰ به‌صورت دو اعتبارِ ٪۱۰ نوشته می‌شود و هر خرید یکی را خرج می‌کند) — یک ردیفِ بزرگ‌ترازسقف در هر خرید کامل کنار می‌رفت، برای همیشه، در حالی که `POST /admin/discounts/grant` جوابِ `ok: true` داده بود؛ پس بنیان‌گذار عددِ کلی‌ای که در ذهن دارد را می‌نویسد و دفتر همان چیزی را نگه می‌دارد که موتور واقعاً می‌تواند بپردازد؛ **٪۲۰ «ستون» روی این سقف سوار می‌شود** (دارندهٔ صندلی با اعتبارِ کامل ٪۳۰ ارزان‌تر می‌خرد) و خودش هرگز مصرف نمی‌شود؛ **اعتبارهای نشان در لحظهٔ خرید مشتق می‌شوند** از همان factهایی که دیوار می‌خواند — فقط مصرف نوشته می‌شود (`discount_redemptions`، چسبیده به ردیف `payments`: خرج‌شده یعنی پرداختِ `pending`/`paid`، پس پرداختِ failed/canceled اعتبارش را با خودِ join آزاد می‌کند، بدون هیچ مسیرِ پاک‌سازی که بشود فراموشش کرد)؛ و **موتور اعتبار هرگز نباید فروشگاه را بخواباند** — `startPayment` با catch محافظتش می‌کند و اگر شکست خورد، به قیمتِ لیست می‌فروشد. سطح‌ها: نوارِ بالای دیوار + پولکِ ارزش روی دیسک‌ها + چیپِ هر سطح (`achievements.js`، از بلوک `discount` و `discount_percent`/`discount_state` هر سطح در `GET /achievements`)، خطِ اضافهٔ کارتِ جشن (`discount_percent` در `pendingAnnouncements`)، و `onetime_discount` کنار `pillar_discount` در `GET /pay/plans` (نوتیسِ صفحهٔ قیمت «با همین خرید مصرف می‌شود» را قبل از دکمهٔ پرداخت می‌گوید). تست‌ها: `plus-api/test/achievements.test.ts` + `achievements.dom.test.ts` + `discounts.test.ts` (+ بلوک‌های «ستون» در `pay.test.ts` و `admin.test.ts`).
- **`plus/des-scores.json` — امتیاز شواهد دنت‌کست (DES) و قرارداد نمایشش.** اسپک: `.dentcast/dentcast-evidence-score-v1.5.md`؛ گامی که رکورد می‌سازد: **۴.۱۳** ورکفلو (مسلح‌شده با سوال **۴.۸** فاز B). همان ماشینِ `pathways.json`/`badges.json`: در ریپو نسخه‌بندی می‌شود، ایجنت می‌نویسدش، برای تغییرش deploy لازم نیست. فایل با `content_id` کلید می‌خورد (مسیر صفحه، بدون `/` ابتدایی و بدون `.html`) → `{scored_at, sources:[…]}` و **`sources` آبجکت‌های خروجیِ اسپک را عیناً نگه می‌دارد** — شکل را اسپک تعیین می‌کند. **هیچ چیزِ مشتق‌شدنی کنارش ذخیره نمی‌شود** (نه `band` در سطحِ wrapper، نه `track`، نه رشتهٔ نمایشی): `content_type`، `band`، `question_type` و `provisional` از قبل داخلِ هر آبجکت هستند و نسخهٔ دوم یعنی منبعِ حقیقتِ دوم. فیلدِ brain **نیست** — Hard Rule 5 اجازه نمی‌دهد، و همین دلیلِ وجودِ این فایل است. نبودِ یک `content_id` همان چیزی‌ست که به نمایش می‌گوید چیزی برای نشان‌دادن نیست، پس publishِ اسکیپ‌شده **هیچ کلیدی** نمی‌نویسد، نه placeholder. **قواعد نمایش** (ماکت‌های تأییدشده ۲۰۲۶-۰۸-۱۴): دقیقاً دو جایگاه — یک **چیپ** باریک زیر لید کنار تاریخ، و یک **کارت ارزیابی** بعد از متن و قبل از منبع؛ هیچ‌چیز وسطِ متن و هیچ‌چیز شناور. باند به‌صورت **پنج بلوکِ گسستهٔ E→D→C→B→A که فقط بلوکِ فعلی رنگی‌ست** کشیده می‌شود، نه عقربه و نه نوارِ پیوسته، چون خروجی پیوسته نیست و عقربه به اختلافِ دوامتیازی معنایی می‌دهد که ندارد. **`question_type` به باند جوش خورده و هرگز جدا از آن نمایش داده نمی‌شود** — باندها فقط داخلِ یک نوع سؤال قابل‌مقایسه‌اند، پس A در یک بنچ‌تست و A در یک کارآزمایی بالینی از دو جدولِ جدا می‌آیند و نباید برابر خوانده شوند. عددِ ۰ تا ۱۰۰ **کوچک و کم‌رنگ کنارِ باند** می‌آید، هرگز به‌عنوان تیتر: باند چیزی‌ست که با آن قضاوت می‌شود، عدد لایهٔ ممیزی‌ست. امتیازِ `ABSTRACT_ONLY` (`provisional: true`) بلوکِ **هاشورخورده** + نشانِ «امتیاز مقدماتی» می‌گیرد، نه بلوکِ توپر. **محتوای تألیفیِ خودِ دنت‌کست اصلاً میلهٔ باند نمی‌گیرد** — یک بجِ «تجربهٔ بالینی» با امتیاز شفافیت (مثلاً ۱۷ از ۱۹) و فهرستِ چهار معیارِ شفافیت، و باند E پشتِ دکمهٔ جزئیات می‌ماند، نه حذف. رنگ مقیاسِ سومِ خودش است، **خاکستری → سبزِ تیره، هرگز از مسیرِ قرمز** — E خاکستری‌ست نه قرمز، چون باندِ پایین نمرهٔ مردودی نیست و نباید «رد شده» خوانده شود؛ هرگز کهربایی (که یعنی «این چیزی‌ست که اشتراک می‌خرد») و هرگز آبیِ برند. هر جزء (چیپ، باند، نوع سؤال، عدد، نشانِ مقدماتی، معیارهای شفافیت) با یک پاپ‌آپِ کوتاه خودش را توضیح می‌دهد. **هیچ صفحه‌ای مارک‌آپِ DES ندارد** — همان قاعدهٔ ردیفِ اکشنِ مقاله، پس رندرر یک ماژولِ مشترک است که این فایل را می‌خواند و گام ۴.۱۳ فقط داده می‌نویسد. صفحهٔ اصلی یک جعبهٔ توضیحِ دائمی دارد (`#dcDesLegendBtn`، پوستهٔ موبایل، بعد از نوارِ آمار) تا خواننده‌ای که بجِ DES را روی یک مقاله می‌بیند از قبل بداند چیست.
- **کد معرف** (`plus-api/src/services/referrals.ts`، مهاجرت 0044؛ سندِ تحویل `.dentcast/referral-handoff.md`). هر کاربرِ واردشده می‌تواند **یک** کد بسازد — یک aliasِ لاتینِ کوچک (`^[a-z]{4,16}$`، بدون رقم و بدون فارسی — همان مسئلهٔ ی/ي، ک/ك و ZWNJ که `foldFa`ی `hl-view.js` برایش وجود دارد) به‌علاوهٔ دو رقم آخرِ موبایلِ خودش، مثلِ `dentmaster61` (حسابِ بی‌موبایل — تلگرام‌اول — دو رقمِ تصادفی می‌گیرد؛ فقط تزئینی‌ست، یکتایی روی کلِ کد است). **دائمی، بدون تغییرِ نام، بدون ساختِ دوباره** — کد در واتساپ و استوری پخش می‌شود و کدی که عوض شود یا لینک‌ها را می‌کُشد یا سرقتِ کدِ تازه‌پخش‌شده را ممکن می‌کند. هرکس کد را **در صفحهٔ پرداخت و روی اولین خریدِ واقعیِ خودش** بزند ٪۱۰ تخفیف می‌گیرد؛ صاحبِ کد ٪۵ اعتبارِ یک‌بارمصرف می‌گیرد — ولی فقط وقتی معرَّف واقعاً یک ردیفِ `payments` با `status='paid'` داشته باشد (تنها قاعدهٔ ضدِ مزرعه‌داری؛ صرفِ زدنِ کد هیچ اعتباری نمی‌سازد). **موتورِ تخفیفِ جدیدی ساخته نمی‌شود**: کد معرف یک منبعِ **سوم** برای `DiscountCredit`ِ `services/discount-credits.ts` است (`kind: 'referral'`، کنارِ `badge:`/`grant:`)، از همان `availableCredits()`/`pickCredits()`یی می‌گذرد که هر اعتبارِ دیگر می‌گذرد — پس زیرِ همان سقفِ `CREDIT_CAP_PERCENT` با اعتبارهای نشان/اهدایی جمع می‌شود، و ٪۲۰ِ دائمیِ «ستون» **روی** همان سقف می‌نشیند (معرفِ دارای صندلی می‌تواند به ٪۳۰ برسد). چیزی که **نوشته** می‌شود یک ردیفِ `referrals` است (X، Y را معرفی کرد) — هر دو درصد از همان مشتق می‌شوند، با یک استثنا: خودِ درصدها روی ردیف مهر می‌شوند (نه از ثابت‌های `REFERRED_DISCOUNT_PERCENT`/`REFERRER_DISCOUNT_PERCENT` خوانده شوند) تا تغییرِ بعدیِ ثابت‌ها اعتبارِ قبلاً کسب‌شده را عقب نبرد. `referrals.referred_user_id` **یکتاست** — هر حساب در تمامِ عمرش فقط یک بار معرفی می‌شود — و معرَّف فقط روی **اولین خریدِ خودش** می‌تواند کد بزند (هر ردیفِ `paid`ِ قبلی هر کدی را رد می‌کند، حتی کدِ استفاده‌نشده). `POST /pay/start`ِ `referral_code` را `services/payment.ts`ِ `startPayment` **قبل از** `availableCredits()` claim می‌کند، بیرونِ catchِ خودِ موتورِ اعتبار (کدِ نامعتبر خطای کاربر است — ۴۰۰ — نه فروشِ بی‌صدا با قیمتِ کامل)؛ `GET /pay/plans?ref=` فقط **پیش‌نمایش** می‌دهد (`checkClaim`، هرگز `claimReferral`) و برای مهمانِ واردنشده نادیده گرفته می‌شود، تا این روت هرگز فضای کد را شمارش‌پذیر نکند. معرف فقط **عدد** می‌بیند، هرگز اسم (`GET /referral`ِ `used_count`/`purchased_count`/`earned_percent`) — همان احتیاطی که up-board و نخ‌های عمومیِ پشتیبانی از قبل رعایت می‌کنند. `services/merge-profiles.ts` باید هم `referral_codes` و هم هر دو ستونِ `referrals` را حمل کند (وگرنه cascade-deleteِ روی profiles بی‌صدا تاریخچه و اعتبارِ واقعی را پاک می‌کند) و باید هر ردیفی که با ادغام خودمعرف می‌شود را **حذف** کند نه صرفاً repoint، چون Postgres قیدِ `check (referrer_user_id <> referred_user_id)` را per-row و بلافاصله اجرا می‌کند و راهی برای «بعداً درستش کن» وسطِ خودِ `UPDATE` نیست. سطح‌ها: بخشِ «کد معرف»ِ پروفایل (فرمِ ساخت با پیش‌نمایشِ زنده + تأییدِ صریحِ غیرقابل‌تغییر، یا کد + کپی + لینکِ اشتراک‌گذاری + آمار بعد از ساخت) و جعبهٔ کد معرفِ صفحهٔ قیمت بینِ گریدِ طرح‌ها و دکمهٔ پرداخت — `?ref=` پیش‌پرش می‌کند، و مسیرِ ورود-برای-پرداخت کدِ تایپ‌شده را قبل از `location.reload()` در URL می‌نویسد تا گم نشود. تست‌ها: `plus-api/test/referrals.test.ts`.
- **پشتیبانی — تیکت‌ها** (`plus-api/src/services/support.ts`، مهاجرت 0041). اولین کانالی که حرفِ خواننده را **به داخلِ** سایت می‌آورد: هرچه لایهٔ اعلان تا امروز داشت یک‌طرفه بود (اطلاعیه، پوش، برادکستِ بنیان‌گذار)، پس خواننده‌ای که سؤالی داشت فقط صفحهٔ تماس را داشت و جواب در پیام‌رسانی اتفاق می‌افتاد که مالِ هیچ‌کس نبود. **اینکه چه کسی می‌تواند تیکت باز کند را «دسته» تعیین می‌کند، نه پلن.** بستنِ کلِ قابلیت پشتِ پریمیوم درست به نظر می‌رسد تا وقتی یک مورد را دنبال کنی: کسی که تخفیف دانشجویی می‌خواهد **به این دلیل** می‌خواهد که پریمیوم نیست، پس درِ پریمیومی همان تیکتی را رد می‌کند که می‌توانست مشترک بیاورد. `POST /pay/gift` قبلاً همین را حل کرده بود — `requireAuth` است، هرگز `requirePremium`. پس `billing`/`student`/`bug` برای هر کاربرِ واردشده باز است؛ `support` (دستهٔ بازِ مشاوره) همچنان پریمیوم است ولی در ۱۴۰۵-۰۵-۲۱ از `FORM_KINDS` بیرون رفت — مسیرِ سؤالِ پریمیوم حالا گفت‌وگوی زیر مطلب است، و `support` فقط برای این در `TICKET_KINDS` مانده که `kindTitle()` بتواند تیکت‌های قدیمی را همچنان درست عنوان کند. `GET /support/kinds` هنوز وضعیتِ `locked` هر دسته را می‌گوید (دیگر برای هیچ‌کدام از دسته‌های فرم موضوعیت ندارد — هیچ‌کدام پریمیوم نیست). `support_tickets.has_photo` (مهاجرت ۰۰۴۳) ادعایی‌ست که کاربر می‌کند نه چیزی مشتق‌شده — دقیقاً هم‌جنسِ ستونِ `status` — با تیکِ اختیاریِ «عکسی دارم» روی فرم ثبت می‌شود، و همین به هر دو صف اجازه می‌دهد نشانِ «📎 عکس در راه» را نشان دهند تا عکس غافلگیرشان نکند. **یک ریلِ پرداختِ دوم — واریز به شبا — روی همان جدولِ `gift_redemptions`ی می‌چرخد که ریلِ گیفت‌کارت از قبل استفاده می‌کرد** (`kind='bank_transfer'`، ستونِ `amount_rial`ِ مهاجرتِ ۰۰۴۳، و ایندکسِ «یک درخواستِ باز» که حالا per-kind است نه سراسری): `POST /pay/bank-transfer` مبلغ را سمتِ سرور از `PLAN_PRICES_RIAL` حساب می‌کند و هرگز مبلغِ کلاینت را نمی‌پذیرد؛ تأیید همان `approveRedemption`/`rejectRedemption`ی را صدا می‌زند که صفِ گیفت‌کارت هم صدا می‌زند؛ و دکمهٔ «تأیید + اهدای نشان دانشجو»ی پنلِ ادمین (`POST /admin/bank-transfer/approve-with-badge`) تأیید، فعال‌سازیِ اشتراک و اهدای نشانِ `student` را در **یک تراکنش** انجام می‌دهد (`activateMonths`/`grantBadge` هر دو حالا یک کلاینتِ بیرونیِ اختیاری می‌پذیرند، دقیقاً برای همین). نشانِ `student` (`plus/badges.json`، کلاسِ اهدایی کنارِ `companion`) یک یادگاری‌ست، نه فعال‌کننده: ماه‌ها از تأییدِ واریز می‌آیند نه از نشان، پس فارغ‌التحصیل در منطقِ محصول تا ابد «دانشجو» نمی‌ماند. **هیچ‌چیزِ قابلِ‌اشتقاق ذخیره نمی‌شود** (همان دکترینِ دیوار نشان‌ها و `last_item_at` کالکشن‌ها): نه `updated_at`، نه `message_count`، نه ستونِ «منتظرِ چه کسی» — نوبتِ چه کسی است **همان** نویسندهٔ آخرین پیام است و در تنها `SUMMARY_SELECT`ی حساب می‌شود که هر دو صف از آن می‌خوانند، پس فهرستِ خواننده و صفِ بنیان‌گذار نمی‌توانند با هم اختلاف پیدا کنند. تیکت و اولین پیامش با هم کامیت می‌شوند. **کد پیگیری اصلِ ماجراست، نه تزئین** (`services/reference.ts`، مشترک با گیفت‌کارت — یک الفبا با یک دلیل: بدون 0/O و 1/I/L و 5/S و 8/B، چون آدم آن را بلند می‌خواند و در جعبه‌ای تایپ می‌کند که مالِ ما نیست). هیچ‌جای این سیستم آپلود ندارد و این عمدی‌ست: نه آبجکت‌استوریجی هست، و کارت دانشجویی سندِ هویتی‌ست — نگه‌داشتنش بدهی‌ست نه دارایی. عکس با `T-ABC-DEF`ِ نوشته‌شده زیرش به پیام‌رسان می‌رود و `GET /admin/support/by-reference/:ref` همان را به یک حساب تبدیل می‌کند — همان حرکتی که gift-redemption با کدِ کارت می‌کند، و دلیلِ اینکه **بعد از تأیید عکس پاک می‌شود و فقط تصمیم می‌ماند**. سطحِ بنیان‌گذار بلوکِ «صندوق پشتیبانی» روی صفحهٔ `GET /admin` است (صف طوری مرتب می‌شود که هرکس بیشتر منتظرِ آدم مانده بالاتر باشد، گفت‌وگو و پاسخ و «ارسال و بستن» همان‌جا، به‌اضافهٔ جست‌وجوی کد پیگیری) روی `GET /admin/support` و `POST /admin/support/:id/reply|close|reopen`. سطحِ خواننده `/plus/support.html` است؛ گیتش عمداً گیتِ پریمیومِ بقیهٔ صفحه‌های `/plus` **نیست** — هر کاربرِ واردشده نمای واقعی را می‌بیند. پاسخ با نوعِ `support_reply` اعلان می‌شود که در `notify-policy.ts` **بدون سقف** است، با همان استدلالِ هشدارِ تمدید: این جوابِ سؤالی‌ست که خودِ خواننده پرسیده، و یادآوریِ استریکی که صبح رسیده نباید دلیلِ نرسیدنش باشد. `SUPPORT_ALERT_PHONE` بنیان‌گذار را از تیکت تازه خبر می‌کند (خالی یعنی فقط یک خط لاگ — هشداری که کسی نمی‌گیرد هرگز نباید نوشتنِ خواننده را بشکند). تست‌ها: `plus-api/test/support.test.ts`. **گفت‌وگوی زیر مطلب** (مهاجرت 0042) همان شیء است با یک لنگر، نه سیستم دوم: ستون `content_id` روی `support_tickets` **خودِ** تمایزدهنده است (null یعنی از صفحهٔ پشتیبانی نوشته شده)، پس یک صف، یک مسیر پاسخ، یک نوع اعلان. نوشتن فقط پریمیوم است و این‌جا این گیت کاملاً درست است — برخلاف دسته‌های پشتیبانی، هیچ‌کس برای مشترک‌شدن لازم نیست زیر مطلب بنویسد. **خصوصی پیش‌فرض است و عمومی‌کردن یک تصمیم است** (`POST /admin/support/:id/publish`، دکمهٔ «عمومی کن» روی گفت‌وگو): این ترس از کامنت نیست، این چیزی‌ست که فیچر را در مقیاس ۴۴۴ مقاله در برابر یک جمعیتِ پریمیوم کار می‌اندازد — جعبهٔ کامنتِ معمولی زیر تقریباً هر صفحه برای همیشه خالی می‌ماند و این «سایتِ مرده» خوانده می‌شود نه «سایتِ آرام»، و همان کلید اتفاقاً مدیریت (هیچ نوشتهٔ غریبه‌ای پیش‌فرض منتشر نمی‌شود) و مسئولیتِ پزشکی (سؤال بالینی و جوابش قبل از عمومی‌شدن خوانده می‌شوند) را هم حل می‌کند. همان حرکتِ `badge_grants`/`pillar_grants`: چیزی که **نوشته** می‌شود تصمیمِ بنیان‌گذار است. چهار پیامد که کد رویشان بنا شده: **یک خواننده + یک صفحه = یک گفت‌وگو** (ایندکس یکتای جزئی روی `(user_id, content_id)`؛ نظر دوم همان نخ را ادامه می‌دهد و نخِ بسته دوباره باز می‌شود)، **نخ‌های زیر مطلب سهمیهٔ `MAX_OPEN_PER_USER` پشتیبانی را خرج نمی‌کنند** وگرنه نظردادن بی‌صدا جلوی گزارشِ یک مشکلِ پرداخت را می‌گرفت، **`GET /threads/public` تنها اندپوینتِ `routes/support.ts` است که هیچ سشنی نمی‌خواهد** — گفت‌وگوی منتشرشده بخشی از صفحه است، پس **بیرونِ** اسکوپِ کپسوله زندگی می‌کند، چون `preHandler` بر هر روتِ آن کانتکست اعمال می‌شود صرف‌نظر از ترتیبِ ثبت و «اول اعلامش کن» راهی برای عمومی نگه‌داشتنِ یک روت نیست — و **نامِ نویسنده `display_name` اوست** که پیش‌فرضش یک نامِ مستعارِ فارسیِ تولیدشده است، و همین است که پیشنهادِ عمومی‌کردن را اصلاً قابل‌ارائه می‌کند. به خواننده **قبل از نوشتن** گفته می‌شود که ممکن است عمومی شود (کادرِ نوشتن) و وقتی شد هم خبر می‌گیرد (`notifyPublished`). بلوکش (`plus/js/article-threads.js`) **با تأخیر** پشتِ IntersectionObserver رسم می‌شود و وقتی چیزی منتشر نشده و خواننده هم نمی‌تواند بنویسد **خودش را حذف می‌کند** — عنوان با یک تبلیغِ اشتراک زیرش، روی هر صفحه، محتوا نیست. روی **هر دو** سطح سوار می‌شود (`plus.js`: صفحهٔ مستقلِ مقاله و ستونِ C پوستهٔ دسکتاپ)، و CSSاش به‌جای `dc-article.css` در `plus/plus.css` است دقیقاً به همین دلیل — `dc-nav.js` پلاس‌سی‌اس‌اس را همه‌جا تزریق می‌کند، ولی `dc-article.css` را فقط صفحه‌های مقاله لینک می‌کنند و `index.html` هیچ CSS مشترکی بار نمی‌کند. تست‌ها: `plus-api/test/article-threads.test.ts` + `article-threads.dom.test.ts`.
- **اطلاعیه — صندوقِ درون‌سایتی** (`plus/js/notices.js`، `plus-api/src/services/notices.ts`). هر اعلانی که API می‌سازد از قبل دقیقاً از **یک** تابع رد می‌شد — `sendCapped` در `notify-policy.ts`، «تنها دری که هر اعلان خروجی از آن رد می‌شود» — پس اطلاعیه یعنی همان در، متنِ پیام را هم در همان ردیفِ `notification_log` که از قبل می‌شمرد **بنویسد**. هفت نوع اعلان بدون دست‌خوردنِ هیچ‌کدام از سرویس‌هایشان صاحب خانه شدند. دو چیز را درست می‌کند که به‌شکل باگ دیده نمی‌شدند: کاربری که اجازهٔ push نداده و پیام‌رسان وصل نکرده **هیچ‌چیز** نمی‌گرفت (providerها برای کاربرِ بی‌مقصد بی‌صدا no-op می‌کنند)، و سقفِ روزانه اضافی را **دور می‌ریخت** نه اینکه صف کند، پس روزِ شلوغ حتی برای کسی که کانال داشت خبر گم می‌کرد. حالا سقف فقط تصمیم می‌گیرد پیام **سفر کند** یا نه؛ همیشه می‌رسد. **تنها قاعده‌ای که کل این بنا رویش ایستاده: ستون `delivered` دو شغلِ جدول را از هم جدا می‌کند و هر کوئری‌ای که می‌پرسد «آیا فرستاده شد» آن را دارد** (`sentCountOn`، `alreadySentKindOn`) — جدول شمارندهٔ سقف هم هست، پس شمردنِ ردیف‌های صرفاً درون‌سایتی یعنی سقف خودش را تغذیه کند و بی‌صدا بودجهٔ push کاربر را ببندد. وضعیت خوانده‌شدن **یک watermark** است (`profiles.notices_seen_at`) نه پرچمِ per-row، پس حالتِ نیمه‌خوانده وجود ندارد و retry بی‌اثر است. نشانگرش یک **نقطه روی آیکون حساب** است، هرگز تغییرِ رنگِ خودِ آیکون: آدمک نشانگرِ هویت است (خاکستری = مهمان، آبی = وارد شده) و قرمزکردنش یعنی کاربر دیگر نفهمد وارد شده یا نه، و در قراردادِ رابط‌ها یعنی «مشکلی در حسابت هست». داخل همان میزبانِ `overlay.js` رندر می‌شود (مثل پیشخوان و پروفایل)، نه یک صفحهٔ تازه. **پخش همگانی** (`notice_broadcasts`، migration 0026) نیمهٔ دوم است: یک ردیف برای همه به‌جای fan-out، و وضعیت خوانده‌شدنِ هر کاربر مجانی از همان watermark می‌آید. دو چیز از آن استفاده می‌کنند. **انتشار هر مطلب همان لحظه، در هر ساعتی، برای همهٔ کاربران در اطلاعیه می‌نشیند** — پنجرهٔ بیداری و سقف روزانه برای محافظت از یک گوشی و یک توکنِ بات‌اند و هیچ‌کدام در نوشتنِ یک ردیف دخالتی ندارند، پس کاربر رایگان دیگر ۲۴ ساعت منتظر دایجست نمی‌ماند و انتشارِ ساعت ۲ بامداد ساعت ۲ بامداد در صندوق است. متنِ اطلاعیه عمداً **کوتاه** است و پالس نیست («مطلب جدید در {بخش}» + عنوان مقاله): پاراگرافِ توضیحِ مقاله جای lock screen است، نه ردیفی که کاربر اسکن می‌کند. پوش‌هایی که بعدش می‌آیند `inbox: false` می‌دهند تا یک انتشار دو بار دیده نشود — ردیفِ شمارنده‌شان (بدون عنوان) همچنان نوشته می‌شود، چون همان ردیف **خودِ شمارندهٔ سقف** است. و **`POST /admin/notices/broadcast`** اطلاعیهٔ شخصیِ بنیان‌گذار است، با فرمی روی همان صفحهٔ `GET /admin`: ردیفِ اطلاعیه همیشه بلافاصله می‌نشیند، ولی `push` مگر با `force` پنجرهٔ بیداری را رعایت می‌کند — «بی‌سقف» یعنی یک broadcast بودجهٔ کاربر را نخورد، نه مجوزِ بیدارکردنِ مردم در ۳ بامداد. **`POST /admin/notices/user`** همان در است برای **یک** خواننده — پیامی که broadcast هرگز نمی‌توانست ببرد، چون باریک‌ترین مخاطبش یک تیرِ کامل است و تشکر از یک نفر با آن یعنی گفتن به همهٔ پریمیوم‌ها که تشکر شده‌اند. ردیفِ اطلاعیه در هر ساعتی بلافاصله می‌نشیند (یک ردیف در یک جدول است؛ هیچ گوشی‌ای با آن بیدار نمی‌شود)، `push` مگر با `force` پنجرهٔ بیداری را رعایت می‌کند، و نوعِ `system` نمی‌گذارد یادداشتِ شخصی از بودجهٔ روزانهٔ پوشِ خواننده خرج شود. عمداً **hold ندارد**: جاروی صبحگاهی روی `notice_broadcasts` راه می‌رود و اطلاعیهٔ شخصی در آن ردیفی ندارد، پس پوشِ خارج از ساعت به‌جای اینکه در ماشینی صف شود که هرگز برش نمی‌دارد، `push_skipped` گزارش می‌شود. دو قاعده مخاطبِ یک broadcast را می‌بندد: `audience` (همه/رایگان/پریمیوم، در لحظهٔ خواندن) و `created_at > profiles.created_at`، تا هیچ‌کس خبری از قبل از وجودِ خودش نبیند؛ `profiles.notices_seen_at` هم به همین دلیل default آن `now()` است. تست‌ها: `plus-api/test/notices.test.ts` + `notices.dom.test.ts`.
- **اعلامِ نشانی که هرگز ذخیره نشده** (`plus-api/src/services/achievement-sync.ts`، `achievement_announcements`). چون هر نشان مشتق می‌شود (بالا، `badges.json`)، هیچ کدی نمی‌تواند بفهمد نشانی *همین‌الان* کسب شد — پس چیزی که ذخیره می‌شود عمداً **خودِ نشان نیست، اعلام است**: دفترِ آنچه تا حالا به این کاربر گفته‌ایم. سه قاعده نمی‌گذارد این همان منبعِ دومِ حقیقتی شود که طراحیِ مشتق ردش کرد: دیوار هرگز از آن نمی‌خواند (`GET /achievements` دست‌نخورده)؛ **high-water mark** است، پس نشانی که خاموش و دوباره روشن می‌شود — «فاتح» با هر انتشارِ تازه که پوشهٔ کامل را زیر ۱۰۰٪ می‌برد — دوباره اعلام نمی‌شود؛ و **هیچ‌چیز عطف به ماسبق نیست**. این آخری کلِ داستانِ روزِ انتشار است و در `profiles.achievements_seeded` زندگی می‌کند: migration 0025 آن را برای هر حسابی که آن روز وجود داشت FALSE می‌کند و DEFAULT را TRUE می‌گذارد — حسابِ قدیمی دقیقاً یک بار به‌صورت خاموش جبران می‌شود (تاریخچه‌اش بایگانی و از پیش تأییدشده، بدون هیچ اطلاعیه‌ای) و حسابی که بعد از آن ساخته می‌شود با دفترِ خالی شروع می‌کند و **اولین نشانش به او گفته می‌شود**. sync با debounce پنج‌دقیقه‌ای و fire-and-forget از مسیرهای نوشتن اجرا می‌شود، per-user ادغام می‌شود، و drain دارد (`drainAchievementSyncs`) تا `truncate` تست‌ها با آن deadlock نکند. کارتِ جشن روی یک **لیستِ سفید** از سطوحِ آرام باز می‌شود — پروفایل و پیشخوان، هرگز صفحهٔ مقاله — چون روی پوستهٔ دسکتاپ مقاله اصلاً صفحه نیست (`index.html` آن را fetch و داخل ستون C تزریق می‌کند) و کاربرِ وسطِ پاراگراف از نظر URL روی صفحهٔ اصلی است، پس لیستِ سیاه دقیقاً در بدترین لحظه صفحه‌اش را می‌پوشاند. تأییدش از watermarkِ اطلاعیه جداست (`POST /achievements/seen`)، پس خواندنِ اطلاعیه هرگز یک جشنِ نمایش‌داده‌نشده را خرج نمی‌کند.
- `plus/js/sheet.js` — باتم‌شیتِ مشترک. اول داخل `collections.js` برای انتخابگرِ بورد نوشته شده بود و وقتی دیوارِ افتخارات همان شیء را خواست به این‌جا منتقل شد؛ به‌جای پیاده‌سازی دوبارهٔ یک شیت، به این ماژول اضافه کن (همان قاعدهٔ `hl-view.js`).
- **دفترچهٔ هایلایت‌ها** (`/plus/highlights.html`، پریمیوم). تنها جایی که کاربر
  **همهٔ** هایلایت‌هایش را یکجا، گروه‌بندی‌شده بر اساس مقاله و همراه یادداشت‌ها
  مرور می‌کند — بدون رفتن به مقاله و بدون «میز کار». دلیل وجودش: پیش از آن
  هایلایت‌ها عملاً یک‌طرفه بودند — داشبورد شش ردیف نشان می‌داد، هر ردیف لینکی به
  مقاله بود، و صفحهٔ مقاله تا وقتی دوباره «میز کار» زده نشود هیچ نشانه‌ای از
  هایلایت‌های کاربر نمی‌کشد (گزارش کاربر، ۲۰۲۶-۰۸-۰۵). بک‌اند:
  `GET /highlights/library` (`requirePremium`) در
  `plus-api/src/routes/highlights.ts`؛ `GET /highlights/recent` هم
  `total`/`article_count` کلِ کتابخانه را در هر پلنی برمی‌گرداند. فرانت:
  `plus/js/highlights.js` (رندرکننده) + `plus/js/highlights-page.js` (گیت). دو
  ثابت: **هر هایلایت یک کارت است، نه لینک** (متن کامل + یادداشت، محتوای همان
  ردیف است؛ «متنِ مقاله» فقط یکی از کنش‌های کوچک کنارش)، و **هر لینک به مقاله
  `?dcphl=<highlight_id>` دارد** — آن‌وقت `plus.js` میز کار را باز می‌کند و
  `Workbench.focusHighlight()` روی همان mark اسکرول می‌کند و لحظه‌ای می‌درخشاندش.
  فهرست اخیرِ داشبورد و pinهای هایلایتِ کالکشن هم با همین لینک می‌روند. دو ثابت
  دیگر: **یادداشت/برچسب/رنگ همان‌جا ویرایش می‌شوند**
  (`inlineEditor` در `hl-view.js` → `PATCH /highlights/:id`)، و **همهٔ فیلترها
  در URL می‌نشینند** (`?q&label&color&folder&sort&view` با `replaceState`) تا یک
  نمای فیلترشده با رفرش و دکمهٔ بازگشت از بین نرود. دو نما (گروه‌بندی مقاله /
  فهرست زمانی)، چیپ‌های برچسب با شمارش، فیلترِ رنگ، فهرست پرش بین مقاله‌ها،
  میان‌برِ `/` برای جستجو، کپیِ تکی/مقاله/نتایج، و رندرِ تدریجی با
  `IntersectionObserver` هم دارد. آنچه پریمیوم می‌خرد **نمای تجمیعی** است، نه
  دسترسی به دادهٔ خود کاربر: شش ردیف داشبورد، میز کارِ هر مقاله و
  `GET /export/highlights` روی همهٔ پلن‌ها می‌مانند.
- **`plus/js/hl-view.js`** — واژگانِ مشترکِ هر سطحی که یک هایلایتِ ذخیره‌شده را
  نشان می‌دهد (دفترچه، بردِ کالکشن، و هرچه بعداً بیاید): `hlMark`/`noteBlock`/
  `labelChip`/`actionBtn`، `foldFa` (فولدینگِ فقط-برای-جستجو روی ZWNJ و ی/ک عربی
  — هرگز روی متنِ ذخیره‌شده)، `highlightHref` (لینکِ `?dcphl=`)، `toast`،
  `skeleton`، `confirmStrip` و `inlineEditor`. به‌جای ساختنِ کارتِ تازه، به این
  ماژول اضافه کن.
- **کالکشن‌ها** (فاز ۳ پریمیوم؛ `collections`/`collection_items` اسپک §۴، از
  migration 0001 provision شده، از ایندکس‌های migration 0012 زنده). برخلاف
  pathway (منتخبِ بنیان‌گذار) یا آرشیو موضوعی (گروه‌بندی خودکار با تاکسونومی)،
  کالکشن **کاملاً مال خود کاربر** است — هر ترکیبی از highlightها و صفحه‌های کامل،
  بدون وابستگی به تاکسونومی. بک‌اند: `plus-api/src/routes/collections.ts` (CRUD
  کامل + افزودن idempotent آیتم، همه `requirePremium`). فرانت:
  `/plus/collections.html`، `/plus/collection.html?id=`، بلاک «کالکشن‌ها» در
  داشبورد `/plus/`، و دکمه‌های **«افزودن به کالکشن»** — دکمهٔ دوهدفه در تولبار
  workbench (highlight انتخاب‌شده، یا کل صفحه اگر چیزی انتخاب نشده — همان الگوی
  دکمهٔ یادداشت) و دکمهٔ هر ردیف در فهرست highlightهای اخیرِ داشبورد. هر سه از
  `openCollectionPicker()` در `plus/js/collections.js` می‌گذرند که پیام گیتِ
  رایگان را هم خودش نشان می‌دهد (روی کلیک، نه صفحهٔ قفل). **pin یک کارت است، نه
  لینک** — همان قاعدهٔ دفترچه، با همان محتوا (mark، یادداشت، برچسب) به‌علاوهٔ
  کنش‌های خودش: ویرایشِ درجا، کپی، **انتقال** به بردِ دیگر، «متنِ مقاله ›»
  (`?dcphl=`) و حذف. انتقال اندپوینت نمی‌خواهد: `openCollectionMove()` اول به
  مقصد اضافه می‌کند و بعد حذفِ خودِ برد را صدا می‌زند؛ چون افزودن در سطح دیتابیس
  idempotent است، بدترین حالتِ نیمه‌کاره این است که آیتم در هر دو برد بماند، نه
  اینکه گم شود. کاتالوگ جستجو + مرتب‌سازی دارد و هر برد `item_count` و
  `last_item_at` را نشان می‌دهد که `GET /collections` آن را **مشتق** می‌کند
  (`max(ci.created_at)`) نه ذخیره — بدون ستون، بدون migration. صفحهٔ برد هم
  فیلترِ نوع (هایلایت/صفحه) و متن دارد. هر دو سطح از `hl-view.js` استفاده
  می‌کنند و شیتِ انتخاب (`chooserCard`) یک کامپوننت است برای «افزودن» و
  «انتقال». migration **۰۰۱۸** هویتِ خودِ برد را اضافه می‌کند
  (`emoji`/`color`/`description`، همه اختیاری — بردی که هیچ‌کدام را ندارد دقیقاً
  مثل قبل رندر می‌شود) و `collection_items.position` را: **NULL یعنی «هرگز
  چیده نشده»**، پس بردِ دست‌نخورده هنوز تازه‌ترین-اول باز می‌شود، و
  `PUT /collections/:id/items/order` **کلِ** ترتیب را یکجا می‌نویسد (آرایهٔ خالی
  پاکش می‌کند) — برای همین حالتِ نیمه‌چیده وجود ندارد و تکرارِ درخواست بی‌اثر
  است. «چیدمان» در UI یک **مود** است (`⇅ چیدمانِ دستی`): ماسونری را تک‌ستونه
  می‌کند، فیلترها را کنار می‌گذارد (↑/↓ روی زیرمجموعهٔ فیلترشده آیتم را از روی
  کارت‌هایی که دیده نمی‌شوند رد می‌کند) و به هر pin دکمهٔ ↑/↓ و «۲ از ۷» می‌دهد —
  دکمه، نه drag، چون کشیدنِ کارتِ ماسونری روی موبایل غیرقابل‌استفاده و برای
  کیبورد نامرئی است. migration **۰۰۳۶** (از ۰۰۳۵ی هندآف شماره‌عوض‌کرده — `0035_ai_card_state` زودتر merge شد و شماره مال اوست) دو نوع pin که برد خودش می‌سازد
  (چوزرِ «افزودن پین» روی هدر برد) + خروجی برد را اضافه می‌کند — دفترِ طراحی در
  `.dentcast/collections-pins-export-handoff.md` است و تصمیم‌هایش گرفته شده، نه
  باز: **pin متنی «متن خودم»** و **pin «رفرنس»** در جدول `snippets` زندگی
  می‌کنند (نه "notes" — آن اسم مال `article_notes` است؛ یک ردیف، چند pin از
  طریق `collection_items.snippet_id`، ایندکس یکتای جزئیِ سوم و چکِ یک‌شکلی
  XOR)، با `POST /collections/:id/snippets` اتمیک ساخته-و-پین می‌شوند، با
  `PATCH /snippets/:id` ویرایش (kind تغییرناپذیر) و با همان `POST .../items` با
  `{snippet_id}` دوباره‌پین/منتقل. مشخصات DOI رفرنس **در مرورگرِ خود کاربر** از
  Crossref می‌آید (اگرسِ بین‌المللیِ کانتینر نامطمئن است؛ API فقط ذخیره می‌کند)
  و ورودِ دستی مسیرِ درجه‌یک است نه fallback — مجله‌های فارسی اکثراً DOI
  ندارند. **قانون یتیم**: حذفِ آخرین pin یک snippet خودِ snippet را در همان
  تراکنش حذف می‌کند (صفحهٔ کتابخانهٔ snippet وجود ندارد، پس snippet بی‌pin
  برای همیشه نامرئی-اما-زنده می‌ماند)؛ «انتقال» هرگز آن را فعال نمی‌کند چون
  اول به مقصد اضافه می‌کند بعد حذف. XP جدیدی وجود ندارد (pinها همان
  `collection_item_added` را می‌زنند؛ «گنجینه» خودکار می‌شمرد) و
  `GET /export/highlights` در **هر** پلن `snippets` را هم دامپ می‌کند (مالکیت
  داده). **خروجی برد** («خروجی» روی هدر برد):
  `GET /collections/:id/export?format=docx|pptx` فایل را سمتِ سرور در حافظه
  می‌سازد (docx + pptxgenjs، JS خالص، استریم، بدون فایل موقت روی دیسکِ
  ephemeral) به ترتیبِ نمایشِ خودِ برد — چیدمانِ دستی همان ترتیبِ
  اسلاید/جزوه است. رفرنس‌ها در بخش شماره‌دارِ «منابع» جمع می‌شوند (ونکووری،
  با همان گاردِ `withDot` کپی‌استنادِ کلاینت)؛ RTL پاراگراف‌به‌پاراگراف است
  (`bidirectional`/`rtl="1"` + راست‌چین برای فارسی، LTR ساده برای استناد
  لاتین). pptx گیتِ فایل-واقعیِ §4.3 هندآف را در ۲۰۲۶-۰۸-۱۰ رد کرد؛ نقصِ
  شناخته و پذیرفته: ویوئرهای Docs/Slides گوگل پرچم rtl را نادیده می‌گیرند
  (بولت آن‌جا چپ می‌افتد) ولی پاورپوینتِ واقعی رعایت می‌کند. دیپلوی این‌ها
  **rebuild ایمیجِ کانتینر API** می‌خواهد (dep جدید npm + migration 0036)، نه
  رفرشِ محتوا.

---

## ۸. ثابت‌های سراسری سایت

- **Google Analytics 4 (deferred) روی هر صفحه.** هر صفحهٔ HTML باید اسنیپت
  deferred GA4 (شناسهٔ `G-GMM0WC8X3M`) را داخل `<head>` داشته باشد. lazy-load است
  — `gtag.js` فقط بعد از رویداد `load` اضافه می‌شود تا first paint را بلاک نکند.
  از اسنیپت پیش‌فرضِ async گوگل **استفاده نکن**؛ الگوی زیر را بگذار. چون صفحهٔ
  جدید از آخرین صفحهٔ هم‌دسته کلون می‌شود، تگ خودکار منتقل می‌شود — اما همیشه
  تأیید کن که از کلون جان سالم به در برده.
  - تزریق‌کنندهٔ کانونی: `.github/scripts/inject_ga.py` (idempotent؛ صفحه‌هایی که
    دارند و صفحه‌های بدون `<head>` را رد می‌کند). بک‌فیل/تأیید کل سایت:
    `python3 .github/scripts/inject_ga.py` (یا `--check` برای fail در نبودش).
  - تمپلیت‌های بیلد هم همین را emit می‌کنند: `GA_DEFERRED_SNIPPET` در
    `tools/build_episodes.py` و `tools/build_pillar.py` (و literal هدِ glossary).
    اگر اسنیپت عوض شد، همه را هم‌گام نگه دار.
  - کدبیس‌های `.org` و `.ir` آینهٔ هم‌اند؛ همین تگ عمداً روی هر دو deploy می‌شود.
    منطق per-domain **اضافه نکن**.
  - اسنیپت دقیق (داخل `<head>`):
    ```html
    <!-- Google Analytics (deferred: loads only after the page is fully rendered) -->
    <script>
      window.addEventListener('load', function () {
        window.dataLayer = window.dataLayer || [];
        function gtag(){ dataLayer.push(arguments); }
        window.gtag = gtag;
        gtag('js', new Date());
        gtag('config', 'G-GMM0WC8X3M');
        var ga = document.createElement('script');
        ga.async = true;
        ga.src = 'https://www.googletagmanager.com/gtag/js?id=G-GMM0WC8X3M';
        document.head.appendChild(ga);
      });
    </script>
    ```
  - اگر روزی CSP اضافه شد، باید `script-src https://www.googletagmanager.com` و
    `connect-src https://*.google-analytics.com https://*.analytics.google.com`
    را اجازه بدهد. (فعلاً هیچ CSPای در این ریپو نیست.)

- **preconnect به API روی همهٔ صفحه‌ها.** هر صفحهٔ HTML در ابتدای `<head>` دو
  لینک `preconnect` به `api.dentcast.ir` و `api.dentcast.org` با `crossorigin`
  دارد. تزئینی نیست، مسئلهٔ درستیِ اسپانسر پولی است: کارت Spot تا جواب `/me`
  رندر نمی‌شود (همان قفلی که «پریمیوم تبلیغ نمی‌بیند» را واقعی می‌کند و
  **نباید** شل شود)، پس در صفحهٔ اول هر تب هندشیکِ سردِ DNS+TLS دقیقاً جلوی
  ایمپرشن اسپانسر می‌نشست — اندازه‌گیری‌شده ۲.۷ تا ۳ ثانیه تا اولین کارت، در
  برابر ۷۰ تا ۱۷۰ میلی‌ثانیه در ریفرش.
  - تزریق‌کننده: `.github/scripts/inject_preconnect.py` (idempotent، با
    `--check`). `tools/build_pillar.py` مقدار `PRECONNECT_SNIPPET` را می‌نویسد و
    `tools/episodes_template.html` آن را درون‌خطی دارد.
  - **`crossorigin` الزامی است**: API با credentials صدا زده می‌شود و اتصالِ
    ناشناس برای درخواست CORSِ credentialed بازاستفاده نمی‌شود.
  - هر دو میزبان روی همهٔ صفحه‌ها عمدی‌اند — دو کدبیس آینهٔ هم‌اند و منطق
    per-domain ممنوع است.

- **کهربایی رنگِ پریمیوم است — یک رنگ، یک معنی.** کهربایی/طلایی (`#c9922b`
  در روشن، `#e3b849` در تیره؛ `#c49820` قدیمی هم از همین خانواده است) فقط
  چیزی را نشان می‌دهد که **اشتراک می‌خرد و بس**: کارت‌های قفل‌شده‌ی
  «دنت‌کست پریمیوم» در صفحه‌ی اصلی، بَج «🔒 ویژه‌ی پریمیوم»، و کارت
  کتابخانه در آرشیو. بقیه‌ی سایت آبیِ خودش را نگه می‌دارد. دو نتیجه که هر
  دو یک‌بار اشتباه شده‌اند: **پالس** خبر است نه پریمیوم، پس با وجود
  کهربایی تاریخی‌اش آبی می‌ماند؛ و **«دنت‌کست پلاس» بخشِ ویدیوی رایگان
  است، نه سطح پولی** (پلاس = واردشده‌ی رایگان، پریمیوم = پولی)، پس چیپش
  هرگز کهربایی نمی‌شود. توکن‌ها در `<style id="dcDepthTheme">` فایل
  `index.html` هستند (`--x-amber*`)؛ سطح پریمیوم یک شست‌وشوی محو که در ۴۵٪
  بالایش تمام می‌شود + یک خط مویی کهربایی می‌گیرد — نه کارت یک‌دستِ
  کهربایی، که حسِ هشدار می‌دهد.

- **ردیفِ کنشِ مقاله — یک ردیف بالای متن، و هیچ صفحه‌ای مالکش نیست.**
  `#dcActionRow` (`.dc-actions`) هر چیزی را که پیش‌تر بین عنوان و اولین پاراگراف
  می‌نشست در خود دارد. تا ۲۰۲۶-۰۸-۱۲ آن ناحیه **چهار نوارِ مستقل از چهار
  تزریق‌کننده** بود — ردیف چیپِ زمان مطالعه/اشتراک‌گذاری در dc-nav.js، خطِ قلبِ
  votes.js، فهرست مطالبِ dc-nav.js، و نوار میز کارِ plus.js — که هرکدام
  `insertBefore(firstBox)` خودش را می‌زد، پس **ترتیبشان تصادفِ ترتیبِ لودِ
  اسکریپت** بود و ترازها (وسط/وسط/تمام‌عرض/راست) و اندازهٔ دکمه‌ها با هیچ‌چیز
  نمی‌خواند. دو گروه، و همین تقسیم خودِ طراحی است: **`.dc-actions-main`** آنچه
  می‌شود با صفحه انجام داد (میز کار › افزودن به کالکشن › پسندیدم، به همین ترتیب)،
  **`.dc-actions-aux`** آنچه صفحه دربارهٔ خودش می‌گوید (زمان مطالعه) به‌علاوهٔ یک
  ادب (اشتراک‌گذاری). پنج قاعده‌ای که کد به آن تکیه دارد. **هیچ صفحه‌ای این
  مارک‌آپ را ندارد** — صفر از ۱۹۷ صفحهٔ مقاله — که دقیقاً دلیل این است که مرتب
  کردن آن ناحیه هیچ ویرایشِ صفحه نخواست و هر تغییر بعدی هم باید در فایل‌های
  مشترک بماند. **دو سازنده، هر ترتیبی، هر دو find-or-create**: فاز 7b در
  `dc-nav.js` پوسته را می‌سازد و گروه آرام را پر می‌کند (تنها چیزی روی صفحه که
  می‌تواند پیش از لودِ گرافِ ماژول‌ها کلمه بشمارد)؛ `ensureActionRow` در
  `plus.js` گروه اصلی را پر می‌کند و در پوستهٔ دسکتاپ — که dc-nav.js از مقالهٔ
  fetch‌شده حذف است — کلِ ردیف را خودش می‌سازد. **استایل‌ها در `plus.css`
  می‌نشینند، هرگز `dc-article.css`** — پوسته اولی را نگه می‌دارد و دومی را حذف
  می‌کند، و یک ردیف را نمی‌شود در دو استایل‌شیت تعریف کرد. **یک واژگانِ دکمه**
  (`.dc-act` + یک modifier: `-primary` پُر، `-outline`، `-quiet`، `-heart`)، همه
  با یک ارتفاع و یک شکل، تا فقط وزن و رنگ بگوید کدام مهم‌تر است — سه اندازهٔ
  قرصِ متفاوت در یک ردیف دقیقاً همان چیزی بود که شلوغی خوانده می‌شد. و **زمان
  مطالعه دکمه نیست**: به‌صورت `.dc-meta-chip` عیناً شبیه قرصِ اشتراک‌گذاریِ کنارش
  بود و خواننده باید می‌زدش تا بفهمد کاری نمی‌کند. **فهرست مطالب کارتِ خودش
  می‌ماند**، زیر ردیف و بالای متن — تنها چیزی در آن ناحیه که محتواست نه کنترل.
  و این ناحیه در `config.js` **سه** لنگر دارد نه یکی: `findProseBox()` (اولین
  جعبه) برای چیزی که **بالای** مقاله می‌رود، `findProseRoot()` (بدنه به‌عنوان یک
  عنصر) برای اندازه‌گیری، و `findProseEnd()` (آخرین جعبه) برای چیزی که **زیرِ**
  آن می‌رود. روی هر صفحه‌ای که بدنه‌اش یک جعبه است هر سه یکی‌اند، و همین است که
  انتخابِ اشتباه را نامرئی نگه می‌دارد — گفت‌وگوی زیر مطلب با لنگرِ
  `findProseBox()` منتشر شد و روی ۴۰۰ و خرده‌ای صفحه درست نشست، در حالی که روی
  ۲۶ صفحهٔ قدیمیِ نوت‌کست بعد از بخش ۱ از ۸ باز می‌شد. `findProseRoot()` هم
  فیکسش نیست: دقیقاً روی همان صفحه‌ها خودِ `main.article-content-wrap` است، پس
  درج بعد از آن بیرونِ پوستهٔ مقاله می‌افتد.
  **اپیزودهای صوتی ردیفِ کوتاه‌ترِ همین جنس را می‌گیرند** — قلب +
  اشتراک‌گذاری، بدون میز کار (پادکست میز کار ندارد) — که
  `initEpisodeActions()` روی صفحهٔ مستقل و شاخهٔ اپیزودِ
  `mountArticleWorkbench` روی پوستهٔ دسکتاپ می‌سازند — و همان دو فراخوان
  **گفت‌وگوی زیر مطلب** را هم به اپیزود می‌دهند، که فقط به این دلیل به
  مقاله‌ها رسیده بود که `initArticle()` سوارش می‌کند و `initArticle()` سرِ
  پخش‌کنندهٔ صوت کنار می‌کشد. هیچ‌وقت پادکست استثنا نشده بود: کل بلوک روی یک
  `content_id` نوشته شده و API روی چیز دیگری گیت نمی‌گذارد، پس
  دسترس‌ناپذیر بود نه ممنوع. لنگرش آنجا همان تکْ‌`.ep-box` است، که گفت‌وگو را
  زیر کلِ کارتِ اپیزود و بالای ناوبریِ ‹قبلی/بعدی› می‌نشاند. تا ۲۰۲۶-۰۸-۱۲ تنها
  محتوای سایت بودند که خواننده نمی‌توانست پسندیدم‌شان بزند، در حالی که مثل
  بقیه در کاتالوگ up-board نشسته بودند (۲۱۰ از ۴۴۴ مدخل) و تعاملی
  می‌ساختند که امتیاز از قبل می‌شمرد (`episode_listened`). چیزی به
  `tools/episodes_template.html` اضافه نشد: ردیف در تمپلیت یعنی ری‌بیلدِ ۲۰۹
  فایل برای چیزی که ماژول مشترک مجانی سرِ جایش می‌گذارد.

- **نماد اعتماد الکترونیکی (enamad) — سه جایگاه، فقط روی dentcast.ir.** نماد
  دقیقاً روی سه سطح است و عنصر per-page **نیست**: فوتر `index.html` (شلِ
  موبایل)، ردیف `.dcd-a-seal` در سایدبار ستون A همان فایل (شلِ دسکتاپ، جایی که
  آن فوتر `display:none` است)، و `.dc-trustseal-box` در `about.html` زیر کارت
  تماس. روی صفحه‌های محتوایی **کلونش نکن** — هر کپی یک ریکوئست به سرور اینماد
  است با ارزش اعتمادسازیِ صفر در صفحهٔ مقاله.
  - **این تنها استثنای عمدیِ قاعدهٔ «منطق per-domain اضافه نکن» است.** نماد برای
    یک دامنه صادر می‌شود (مالِ ما: `dentcast.ir`) و
    `trustseal.enamad.ir/logo.aspx` بر اساس referrerِ درخواست رندر می‌کند؛ پس
    روی میرورِ `.org` تصویرِ *نماد نامعتبر* برمی‌گرداند. دو لایه گیتش می‌کنند:
    یک گاردِ inline در `<head>` هر دو صفحه که خارج از `.ir` کلاس `dc-no-seal` را
    روی `<html>` می‌گذارد (قبل از پارس‌شدن body پنهانش می‌کند، بدون فلش)، و یک
    بلاک در انتهای `dc-nav.js` که نود را کاملاً حذف می‌کند. تصویرها
    `loading="lazy"` هستند و هر دو نماد زیر fold می‌نشینند، پس روی `.org` معمولاً
    اصلاً درخواستی فرستاده نمی‌شود.
  - **اتریبیوت‌هایی که اینماد اعتبارسنجی می‌کند عیناً کپی می‌شوند و هرگز بازنویسی
    نمی‌شوند** — `referrerpolicy='origin'`, `id`, `Code`, و اتریبیوت `code` روی
    `<img>`. فقط `alt`، `loading` و wrapper مالِ ماست. مارک‌آپ **به‌صورت استاتیک
    در HTML** می‌آید (هرگز با JS تزریق نمی‌شود) تا خزندهٔ خودِ اینماد بخواندش.
  - استایل `.dc-trustseal` در `dc-theme.css` است و در `<style>` درون‌خطیِ
    `index.html` آینه شده (index هیچ CSS مشترکی لود نمی‌کند). چیپ در **هر دو** تم
    سفید است، عمداً — آرت‌ورکِ نماد پس‌زمینهٔ روشن فرض می‌کند — و جعبه‌اش
    اندازهٔ ثابت دارد تا با آمدن تصویر چیزی جابه‌جا نشود.
  - ریدرِ شلِ دسکتاپ `.dc-trustseal` را همراه بقیهٔ chrome صفحه strip می‌کند
    (لیست سلکتورهای حذف chrome در `index.html`): `dc-nav.js` از آن سند حذف
    می‌شود، پس نمادِ داخل iframe از گیتِ دامنه فرار می‌کرد.

---

## ۹. رفتار ایجنت (همسانی خروجی)

قواعد رفتاری — زبان گفت‌وگو، گیت‌های تأییدی که هرگز خودکار نمی‌شوند، نقشهٔ
ابزارها وقتی توانمندی‌ای را نداری، دستورهای قطعیِ هش/بیلد/اعتبارسنجی، چک‌لیست
همسانیِ خروجی، و پروتکل گیت — همه در:

**`.dentcast/workflows/agent-parity.md`**

خلاصهٔ سه بندِ حیاتی‌اش:

1. **فایل کانونی را کامل بخوان، هر بار.** خواندنِ ناقصِ `README.md` (بیش از ۱۲۰۰
   خط) و شروع کار، نقض است.
2. **هرگز حدس نزن.** عدد، تاریخ، DOI، نویسنده، عنوان یا هشتگی که کاربر نداده و
   از فایل/تاکسونومی/منبع واقعی قابل استخراج نیست — پرسیده می‌شود.
3. **گزارش نهایی، قالبِ الزامی است.** فهرست «Final output summary» در انتهای هر
   ورکفلو باید کامل بیاید؛ بندی که مصداق ندارد با جملهٔ صریحِ اسکیپ می‌آید، نه
   با حذف.
4. **«فکر می‌کنم همه‌چیز را انجام دادم» گزارش نیست.** publish با **فاز F**
   (`tools/verify_publish.py`) بسته می‌شود، نه با ادعا. تا وقتی خروجی صفر نشده،
   کار تمام نیست.

---

## ۱۰. نگهداری — چه فایل‌هایی با هم به‌روز می‌شوند

| اگر این عوض شد | این‌ها هم در همان کامیت عوض شوند |
|---|---|
| `CLAUDE.md` | `AGENTS.md` + `.cursor/rules/dentcast-router.mdc` |
| ورکفلوی جدید در `.dentcast/workflows/` | جدول روتر در `CLAUDE.md` + `AGENTS.md` + یک `.mdc` اختصاصی در `.cursor/rules/` |
| فهرست/ترتیب بیلدرها | `.dentcast/workflows/agent-parity.md` بخش ۳ |
| گیت‌های تأیید یا Hard Ruleها | `.dentcast/workflows/agent-parity.md` بخش ۱ و ۴ |
| قاعده‌ای که `tools/verify_publish.py` چکش می‌کند | خودِ اسکریپت + فاز F در `README.md` — قاعده و چکش با هم عوض می‌شوند |

**قاعده: منطق در فایل کانونی، مسیر در روترها، رفتار در `agent-parity.md`.**
هیچ‌کدام محتوای دیگری را کپی نمی‌کند.
